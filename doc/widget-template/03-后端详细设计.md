# 03 · 后端详细设计

> 代码为设计稿（实施依据），非已提交代码。
> 分层遵循项目既有模式：Domain 实体 → Infrastructure 仓储/迁移 → Application DTO/服务 → WebApi 控制器。

---

## 1. 实体 `HmiWidgetTemplate`

文件：`Server/ScadaServer.Domain/Entities/HmiWidgetTemplate.cs`

```csharp
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;

namespace ScadaServer.Domain.Entities
{
    /// <summary>HMI 组件模板：组件库元数据的唯一真相源（替代前端硬编码 widgetRegistry）。
    /// 画布组件（HmiComponent.Type）存 RenderType；本表 TemplateKey 是注册键（两级匹配见前端设计）。</summary>
    [Table("HmiWidgetTemplates")]
    public class HmiWidgetTemplate
    {
        [Key]
        [DatabaseGenerated(DatabaseGeneratedOption.Identity)]
        public int Id { get; set; }

        /// <summary>模板唯一键（= 原注册键，如 title-header-tech-desktop）。唯一索引。</summary>
        [Required]
        [MaxLength(64)]
        public string TemplateKey { get; set; } = string.Empty;

        /// <summary>渲染类型（= 原 ComponentType，如 title-header）。
        /// builtin 轨 = SFC 映射键；svg 轨（D10）= 与 TemplateKey 同值。</summary>
        [Required]
        [MaxLength(64)]
        public string RenderType { get; set; } = string.Empty;

        [Required]
        [MaxLength(100)]
        public string Name { get; set; } = string.Empty;

        /// <summary>分类：equipment | sensors | structures | headers</summary>
        [Required]
        [MaxLength(32)]
        public string Category { get; set; } = "equipment";

        [MaxLength(500)]
        public string Description { get; set; } = string.Empty;

        public int DefaultWidth { get; set; }
        public int DefaultHeight { get; set; }

        /// <summary>图标形态：lucide | div | svg | emoji</summary>
        [Required]
        [MaxLength(16)]
        public string IconKind { get; set; } = "lucide";

        /// <summary>图标标识：lucide 图标名 / div 变体名（div-h/div-v/div-led）/ SVG 源码 / emoji 字符</summary>
        [MaxLength(2000)]
        public string IconKey { get; set; } = string.Empty;

        /// <summary>图标颜色（Tailwind 类，如 text-amber-500；仅 lucide/emoji 生效）</summary>
        [MaxLength(64)]
        public string IconColor { get; set; } = string.Empty;

        /// <summary>渲染轨：builtin（前端 SFC）| svg（通用 SVG 渲染器）</summary>
        [Required]
        [MaxLength(16)]
        public string RenderKind { get; set; } = "builtin";

        /// <summary>RenderKind=svg 时的 SVG 模板源码（已清洗）；其余强制 null</summary>
        [Column(TypeName = "text")]
        public string? SvgTemplate { get; set; }

        /// <summary>默认属性 JSON（原 widgetRegistry.defaultProps() 求值序列化）</summary>
        [Column(TypeName = "text")]
        public string DefaultPropsJson { get; set; } = "{}";

        /// <summary>属性表单 schema JSON（P5 启用）</summary>
        [Column(TypeName = "text")]
        public string PropSchemaJson { get; set; } = "[]";

        /// <summary>系统内置模板（24 条种子）：可编辑/隐藏/排序，禁止删除</summary>
        public bool IsSystem { get; set; }

        /// <summary>组件库显示顺序（升序，同序按 Id）</summary>
        public int SortOrder { get; set; }

        public DateTime CreatedAtUtc { get; set; } = DateTime.UtcNow;
        public DateTime UpdatedAtUtc { get; set; } = DateTime.UtcNow;
    }
}
```

要点：
- `IconColor` 为 v2.0 新增（审查 A3）；
- `TemplateKey` VARCHAR(64) 建**唯一索引**（项目约束：索引列必须显式长度）；
- 无软删（模板删除即物理删，画布实例数据不受影响——`HmiComponent.PropsJson` 是快照）。

## 2. DbContext + 迁移

`ScadaDbContext` 追加：

```csharp
public DbSet<HmiWidgetTemplate> HmiWidgetTemplates => Set<HmiWidgetTemplate>();
```

迁移命令（在解决方案根执行，沿用项目参数惯例）：

```bash
dotnet ef migrations add AddHmiWidgetTemplate --project Server/ScadaServer.Infrastructure --startup-project Server/ScadaServer.WebApi
```

对生成的迁移人工检查 / 调整：

```csharp
protected override void Up(MigrationBuilder migrationBuilder)
{
    migrationBuilder.CreateTable(
        name: "HmiWidgetTemplates",
        columns: table => new
        {
            Id = table.Column<int>(nullable: false).Annotation("MySql:ValueGeneratedOnAdd", true),
            TemplateKey = table.Column<string>(maxLength: 64, nullable: false),
            RenderType = table.Column<string>(maxLength: 64, nullable: false),
            Name = table.Column<string>(maxLength: 100, nullable: false),
            Category = table.Column<string>(maxLength: 32, nullable: false),
            Description = table.Column<string>(maxLength: 500, nullable: true),
            DefaultWidth = table.Column<int>(nullable: false),
            DefaultHeight = table.Column<int>(nullable: false),
            IconKind = table.Column<string>(maxLength: 16, nullable: false),
            IconKey = table.Column<string>(maxLength: 2000, nullable: true),
            IconColor = table.Column<string>(maxLength: 64, nullable: true),
            RenderKind = table.Column<string>(maxLength: 16, nullable: false),
            SvgTemplate = table.Column<string>(type: "text", nullable: true),
            DefaultPropsJson = table.Column<string>(type: "text", nullable: false),
            PropSchemaJson = table.Column<string>(type: "text", nullable: false),
            IsSystem = table.Column<bool>(nullable: false),
            SortOrder = table.Column<int>(nullable: false),
            CreatedAtUtc = table.Column<DateTime>(nullable: false),
            UpdatedAtUtc = table.Column<DateTime>(nullable: false)
        },
        constraints: table => table.PrimaryKey("PK_HmiWidgetTemplates", x => x.Id));

    migrationBuilder.CreateIndex(
        name: "ix_hmiwidgettemplates_templatekey",
        table: "HmiWidgetTemplates",
        column: "TemplateKey",
        unique: true);
}

protected override void Down(MigrationBuilder migrationBuilder)
    => migrationBuilder.DropTable(name: "HmiWidgetTemplates");
```

## 3. DTO

文件：`Server/ScadaServer.Application/DTOs/HmiWidgetTemplateDto.cs`

```csharp
using System.ComponentModel.DataAnnotations;

namespace ScadaServer.Application.DTOs
{
    public class HmiWidgetTemplateDto
    {
        public int Id { get; set; }

        [Required(ErrorMessage = "模板键不能为空")] [MaxLength(64)]
        public string TemplateKey { get; set; } = string.Empty;

        [Required(ErrorMessage = "渲染类型不能为空")] [MaxLength(64)]
        public string RenderType { get; set; } = string.Empty;

        [Required(ErrorMessage = "模板名称不能为空")] [MaxLength(100)]
        public string Name { get; set; } = string.Empty;

        [Required] [MaxLength(32)]
        public string Category { get; set; } = "equipment";

        [MaxLength(500)] public string Description { get; set; } = string.Empty;
        [Range(1, 4096)] public int DefaultWidth { get; set; }
        [Range(1, 4096)] public int DefaultHeight { get; set; }

        [Required] [MaxLength(16)] public string IconKind { get; set; } = "lucide";
        [MaxLength(2000)] public string IconKey { get; set; } = string.Empty;
        [MaxLength(64)] public string IconColor { get; set; } = string.Empty;

        [Required] [MaxLength(16)] public string RenderKind { get; set; } = "builtin";
        public string? SvgTemplate { get; set; }
        public string DefaultPropsJson { get; set; } = "{}";
        public string PropSchemaJson { get; set; } = "[]";
        public bool IsSystem { get; set; }
        public int SortOrder { get; set; }
    }

    /// <summary>导入载荷：单对象或 templates 数组由控制器适配</summary>
    public class WidgetTemplateImportDto
    {
        public string Format { get; set; } = string.Empty;
        public int Version { get; set; } = 1;
        public HmiWidgetTemplateDto Template { get; set; } = new();
        /// <summary>模板不存在时的默认策略：overwrite | rename</summary>
        public string ConflictMode { get; set; } = "rename";
    }

    public class WidgetTemplateExportDto
    {
        public string Format { get; set; } = "scada-widget-template";
        public int Version { get; set; } = 1;
        public HmiWidgetTemplateDto Template { get; set; } = new();
    }

    /// <summary>批量导出载荷（多模板打一个文件）</summary>
    public class WidgetTemplateBundleDto
    {
        public string Format { get; set; } = "scada-widget-template";
        public int Version { get; set; } = 1;
        public List<HmiWidgetTemplateDto> Templates { get; set; } = new();
    }

    public class ImportResult
    {
        public bool Ok { get; set; }
        public int Id { get; set; }
        /// <summary>create | overwrite | renamed | skipped</summary>
        public string Mode { get; set; } = string.Empty;
        public string? NewKey { get; set; }   // rename 时返回实际新键
    }
}
```

## 4. 仓储

接口（追加到 `ScadaServer.Domain/Interfaces/Repositories/IEntityRepositories.cs`）：

```csharp
public interface IHmiWidgetTemplateRepository : IRepository<HmiWidgetTemplate, int>
{
    /// <summary>按唯一键查询（导入冲突检测 / 种子幂等用）</summary>
    Task<HmiWidgetTemplate?> GetByKeyAsync(string templateKey);
}
```

实现（`Infrastructure/Repositories/HmiWidgetTemplateRepository.cs`）：

```csharp
public class HmiWidgetTemplateRepository
    : RepositoryBase<HmiWidgetTemplate, int>, IHmiWidgetTemplateRepository
{
    public HmiWidgetTemplateRepository(ScadaDbContext db) : base(db) { }

    public async Task<HmiWidgetTemplate?> GetByKeyAsync(string templateKey)
        => await Db.Set<HmiWidgetTemplate>()
                   .FirstOrDefaultAsync(t => t.TemplateKey == templateKey);
}
```

> 实施注意：`RepositoryBase` 的增删查改方法签名以实际代码为准（参照 `HmiComponentRepository` 用法），此处仅表达结构。

## 5. SvgSanitizer

文件：`Server/ScadaServer.Application/Common/SvgSanitizer.cs`

```csharp
using System.Text.RegularExpressions;

namespace ScadaServer.Application.Common
{
    /// <summary>SVG 模板清洗（入库前执行；白名单 URL：锚点/内嵌图片/站内相对路径）。
    /// 已知边界（登记不修）：animate attributeName=href 动画钓鱼，风险低。</summary>
    public static class SvgSanitizer
    {
        private const int MaxLength = 256 * 1024;

        private static readonly Regex ScriptRe =
            new(@"<script[\s\S]*?</script>|<script[^>]*/>", RegexOptions.IgnoreCase | Compiled);
        private static readonly Regex ForeignObjectRe =
            new(@"<foreignObject[\s\S]*?</foreignObject>", RegexOptions.IgnoreCase | Compiled);
        private static readonly Regex EventAttrRe =
            new(@"\son\w+\s*=\s*(?<q>""|')(?<v>[\s\S]*?)\k<q>", RegexOptions.IgnoreCase | Compiled);
        private static readonly Regex UrlAttrRe =
            new(@"(?<attr>href|xlink:href|src)\s*=\s*(?<q>""|')(?<v>[\s\S]*?)\k<q>", RegexOptions.IgnoreCase | Compiled);
        private static readonly Regex StyleUrlRe =
            new(@"url\(\s*(?<q>""|')?(?<v>[\s\S]*?)(?(q)\k<q>|[\s)]?)\s*\)", RegexOptions.IgnoreCase | Compiled);

        public static string Sanitize(string svg)
        {
            if (string.IsNullOrWhiteSpace(svg)) return string.Empty;

            var result = ScriptRe.Replace(svg, string.Empty);
            result = ForeignObjectRe.Replace(result, string.Empty);
            result = EventAttrRe.Replace(result, string.Empty);

            result = UrlAttrRe.Replace(result, m =>
                IsSafeUrl(m.Groups["v"].Value) ? m.Value
                    : $"{m.Groups["attr"].Value}={m.Groups["q"].Value}{m.Groups["q"].Value}");

            result = StyleUrlRe.Replace(result, m =>
                IsSafeUrl(m.Groups["v"].Value.Trim()) ? m.Value : "url()");

            return result.Length > MaxLength ? result[..MaxLength] : result;
        }

        private static bool IsSafeUrl(string url)
            => url.StartsWith("#")
            || url.StartsWith("data:image/", StringComparison.OrdinalIgnoreCase)
            || url.StartsWith("/");   // 注意：故意不放行 http(s)，见审查 A12
    }
}
```

与 v1.0 差异（审查 A12）：`Singleline` 语义（`[\s\S]` 显式跨行匹配）+ `style url()` 清洗 + 白名单收紧。

## 6. AppService

接口：`ScadaServer.Application/Interfaces/IHmiWidgetTemplateAppService.cs`

```csharp
public interface IHmiWidgetTemplateAppService
{
    Task<List<HmiWidgetTemplateDto>> GetListAsync();
    Task<HmiWidgetTemplateDto?> GetByIdAsync(int id);
    Task<int> CreateAsync(HmiWidgetTemplateDto dto);
    Task<bool> UpdateAsync(HmiWidgetTemplateDto dto);
    Task DeleteAsync(int id);
    Task<ImportResult> ImportAsync(WidgetTemplateImportDto import);
    Task<WidgetTemplateExportDto> ExportAsync(int id);
    Task<WidgetTemplateBundleDto> ExportBundleAsync(IEnumerable<int> ids);
}
```

实现要点（`Services/HmiWidgetTemplateAppService.cs`）：

```csharp
public class HmiWidgetTemplateAppService : IHmiWidgetTemplateAppService
{
    private static readonly HashSet<string> Categories = new() { "equipment", "sensors", "structures", "headers" };
    private static readonly HashSet<string> IconKinds = new() { "lucide", "div", "svg", "emoji" };
    private static readonly HashSet<string> RenderKinds = new() { "builtin", "svg" };

    private readonly IHmiWidgetTemplateRepository _repository;

    public HmiWidgetTemplateAppService(IHmiWidgetTemplateRepository repository)
        => _repository = repository;

    public async Task<List<HmiWidgetTemplateDto>> GetListAsync()
    {
        var list = await _repository.GetListAsync();
        return list.OrderBy(t => t.SortOrder).ThenBy(t => t.Id)
                   .Select(MapToDto).ToList();
    }

    public async Task<int> CreateAsync(HmiWidgetTemplateDto dto)
    {
        await ValidateAsync(dto);
        var entity = MapToEntity(dto);
        entity.CreatedAtUtc = entity.UpdatedAtUtc = DateTime.UtcNow;
        await _repository.InsertAsync(entity);
        return entity.Id;
    }

    public async Task<bool> UpdateAsync(HmiWidgetTemplateDto dto)
    {
        var entity = await _repository.GetByIdAsync(dto.Id);
        if (entity == null) return false;
        await ValidateAsync(dto);

        entity.TemplateKey = dto.TemplateKey;
        entity.RenderType = dto.RenderType;
        entity.Name = dto.Name;
        entity.Category = dto.Category;
        entity.Description = dto.Description;
        entity.DefaultWidth = dto.DefaultWidth;
        entity.DefaultHeight = dto.DefaultHeight;
        entity.IconKind = dto.IconKind;
        entity.IconKey = dto.IconKey;
        entity.IconColor = dto.IconColor;
        entity.RenderKind = dto.RenderKind;
        entity.SvgTemplate = dto.RenderKind == "svg" ? SvgSanitizer.Sanitize(dto.SvgTemplate ?? "") : null;
        entity.DefaultPropsJson = dto.DefaultPropsJson;
        entity.PropSchemaJson = dto.PropSchemaJson;
        entity.SortOrder = dto.SortOrder;
        entity.UpdatedAtUtc = DateTime.UtcNow;
        await _repository.UpdateAsync(entity);
        return true;
    }

    public async Task DeleteAsync(int id)
    {
        var entity = await _repository.GetByIdAsync(id)
            ?? throw new BusinessException("模板不存在");
        if (entity.IsSystem)
            throw new BusinessException("系统内置模板不可删除");   // 审查 A9：显式报错而非静默
        await _repository.DeleteAsync(entity);
    }

    public async Task<ImportResult> ImportAsync(WidgetTemplateImportDto import)
    {
        if (!string.Equals(import.Format, "scada-widget-template", StringComparison.OrdinalIgnoreCase))
            throw new BusinessException("不支持的模板文件格式");

        var dto = import.Template;
        var existing = await _repository.GetByKeyAsync(dto.TemplateKey);

        if (existing != null && import.ConflictMode == "overwrite")
        {
            dto.Id = existing.Id;
            dto.IsSystem = existing.IsSystem;      // 覆盖不改变系统标记
            await UpdateAsync(dto);
            return new ImportResult { Ok = true, Id = existing.Id, Mode = "overwrite" };
        }

        if (existing != null)
        {
            // 审查 A10：8 位短随机后缀，先截 key 保证后缀完整
            var suffix = Convert.ToBase64String(Guid.NewGuid().ToByteArray())
                .Replace("+", "").Replace("/", "")[..8].ToLowerInvariant();
            var baseKey = dto.TemplateKey.Length > 55 ? dto.TemplateKey[..55] : dto.TemplateKey;
            dto.TemplateKey = $"{baseKey}-{suffix}";
            var created = await CreateAsync(dto);
            return new ImportResult { Ok = true, Id = created, Mode = "renamed", NewKey = dto.TemplateKey };
        }

        var id = await CreateAsync(dto);
        return new ImportResult { Ok = true, Id = id, Mode = "create" };
    }

    public async Task<WidgetTemplateExportDto> ExportAsync(int id)
    {
        var dto = await GetByIdAsync(id) ?? throw new BusinessException("模板不存在");
        return new WidgetTemplateExportDto { Template = dto };
    }

    /// <summary>统一校验：键唯一、枚举白名单、JSON 合法性（审查 A13）</summary>
    private async Task ValidateAsync(HmiWidgetTemplateDto dto)
    {
        if (!Categories.Contains(dto.Category))
            throw new BusinessException($"非法分类：{dto.Category}");
        if (!IconKinds.Contains(dto.IconKind))
            throw new BusinessException($"非法图标形态：{dto.IconKind}");
        if (!RenderKinds.Contains(dto.RenderKind))
            throw new BusinessException($"非法渲染轨：{dto.RenderKind}");
        if (dto.RenderKind == "svg" && string.IsNullOrWhiteSpace(dto.SvgTemplate))
            throw new BusinessException("SVG 渲染轨必须提供 SVG 模板源码");
        if (dto.RenderKind == "svg" && !dto.RenderType.Equals(dto.TemplateKey, StringComparison.Ordinal))
            throw new BusinessException("SVG 模板 RenderType 必须与 TemplateKey 一致（D10）");

        ValidateJson(dto.DefaultPropsJson, "DefaultPropsJson");
        ValidateJson(dto.PropSchemaJson, "PropSchemaJson");

        var dup = await _repository.GetByKeyAsync(dto.TemplateKey);
        if (dup != null && dup.Id != dto.Id)
            throw new BusinessException($"模板键已存在：{dto.TemplateKey}");
    }

    private static void ValidateJson(string json, string field)
    {
        try { using var _ = System.Text.Json.JsonDocument.Parse(
            string.IsNullOrWhiteSpace(json) ? "{}" : json); }
        catch (System.Text.Json.JsonException)
        { throw new BusinessException($"{field} 不是合法 JSON"); }
    }

    // MapToDto / MapToEntity：字段一一映射（略，见实施）
}
```

## 7. Controller

文件：`Server/ScadaServer.WebApi/Controllers/HmiWidgetTemplateController.cs`

```csharp
[ApiController]
[Route("api/[controller]")]
[Authorize]                                    // 读取：所有登录用户（运行态渲染依赖）
public class HmiWidgetTemplateController : ApiControllerBase
{
    private readonly IHmiWidgetTemplateAppService _appService;
    public HmiWidgetTemplateController(IHmiWidgetTemplateAppService appService)
        => _appService = appService;

    [HttpGet]
    public async Task<IActionResult> GetAll()
        => Ok(await _appService.GetListAsync());

    [HttpGet("{id}")]
    public async Task<IActionResult> GetById(int id)
    {
        var dto = await _appService.GetByIdAsync(id);
        return dto == null ? NotFound() : Ok(dto);
    }

    [HttpPost]
    [Authorize(Policy = "RequireAdmin")]
    public async Task<IActionResult> Create([FromBody] HmiWidgetTemplateDto dto)
    {
        var guard = EnsureBody(dto); if (guard != null) return guard;
        var id = await _appService.CreateAsync(dto);
        dto.Id = id;
        return CreatedAtAction(nameof(GetById), new { id }, dto);
    }

    [HttpPut]
    [Authorize(Policy = "RequireAdmin")]
    public async Task<IActionResult> Update([FromBody] HmiWidgetTemplateDto dto)
    {
        var guard = EnsureBody(dto); if (guard != null) return guard;
        return await _appService.UpdateAsync(dto) ? NoContent() : NotFound();
    }

    [HttpDelete("{id}")]
    [Authorize(Policy = "RequireAdmin")]
    public async Task<IActionResult> Delete(int id)
    {
        await _appService.DeleteAsync(id);
        return NoContent();
    }

    [HttpPost("import")]
    [Authorize(Policy = "RequireAdmin")]
    public async Task<IActionResult> Import([FromBody] WidgetTemplateImportDto import)
    {
        var guard = EnsureBody(import); if (guard != null) return guard;
        return Ok(await _appService.ImportAsync(import));
    }

    /// <summary>批量导入：兼容单对象与 templates 数组两种载荷（D11）</summary>
    [HttpPost("import-bundle")]
    [Authorize(Policy = "RequireAdmin")]
    public async Task<IActionResult> ImportBundle([FromBody] WidgetTemplateBundleDto bundle)
    {
        var guard = EnsureBody(bundle); if (guard != null) return guard;
        var results = new List<ImportResult>();
        foreach (var t in bundle.Templates)
            results.Add(await _appService.ImportAsync(
                new WidgetTemplateImportDto { Template = t, ConflictMode = "rename" }));
        return Ok(results);
    }

    [HttpGet("{id}/export")]
    public async Task<IActionResult> Export(int id)
    {
        var dto = await _appService.ExportAsync(id);
        var json = System.Text.Json.JsonSerializer.Serialize(dto,
            new System.Text.Json.JsonSerializerOptions { PropertyNamingPolicy = System.Text.Json.JsonNamingPolicy.CamelCase });
        return File(System.Text.Encoding.UTF8.GetBytes(json), "application/json",
            $"{dto.Template.TemplateKey}.widget.json");
    }

    [HttpPost("export-bundle")]
    public async Task<IActionResult> ExportBundle([FromBody] int[] ids)
    {
        var guard = EnsureBody(ids); if (guard != null) return guard;
        var bundle = await _appService.ExportBundleAsync(ids);
        var json = System.Text.Json.JsonSerializer.Serialize(bundle,
            new System.Text.Json.JsonSerializerOptions { PropertyNamingPolicy = System.Text.Json.JsonNamingPolicy.CamelCase });
        return File(System.Text.Encoding.UTF8.GetBytes(json), "application/json",
            $"widget-templates-{DateTime.UtcNow:yyyyMMddHHmmss}.widget.json");
    }
}
```

> 注意：项目后端成功返回裸 DTO、错误统一 `ApiResponse`（`BusinessException` → 全局异常中间件），本控制器沿用该惯例（与 `HmiComponentController` 的 `Ok(dto)` 一致）。

## 8. DI 注册

- `Server/ScadaServer.WebApi/Extensions/Database.Extensions.cs`（仓储注册区）追加：

```csharp
services.AddScoped<IHmiWidgetTemplateRepository, HmiWidgetTemplateRepository>();
```

- `Server/ScadaServer.WebApi/Extensions/Application.Extensions.cs`（服务注册区）追加：

```csharp
services.AddScoped<IHmiWidgetTemplateAppService, HmiWidgetTemplateAppService>();
```

## 9. 种子数据

`DatabaseInitializer.SeedDataAsync()` 追加调用：

```csharp
await CreateDefaultWidgetTemplatesAsync();
```

实现（幂等模式，与 `CreateDefaultProtocolsAsync` 一致）：

```csharp
private async Task CreateDefaultWidgetTemplatesAsync()
{
    var seeds = BuildBuiltinWidgetTemplates();   // 24 条，数据见 05 文档种子总表
    var existingKeys = await _db.Set<HmiWidgetTemplate>()
        .Select(t => t.TemplateKey)
        .ToListAsync();

    var toAdd = seeds.Where(s => !existingKeys.Contains(s.TemplateKey)).ToList();
    if (toAdd.Count == 0) return;

    await _db.Set<HmiWidgetTemplate>().AddRangeAsync(toAdd);
    await _db.SaveChangesAsync();
    _logger.LogInformation("内置组件模板种子初始化：{Count} 条", toAdd.Count);
}
```

种子数据生成方式：**由 P0 步骤 0.3 的一次性脚本从现有 `widgetRegistry` 序列化生成**（审查 B2），不手写。24 条的完整字段值见 [05-数据协议与种子清单.md](05-数据协议与种子清单.md)。

内置模板后续演进策略（登记）：`baseProps` 逻辑将来若有变化，需同步升级种子（按 key 覆盖 IsSystem 模板的 Name/DefaultPropsJson/SortOrder），提供一次性升级方法即可，不做自动版本比对。

## 10. 接口一览

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | /api/HmiWidgetTemplate | 登录 | 全量列表（按 sortOrder） |
| GET | /api/HmiWidgetTemplate/{id} | 登录 | 单条 |
| POST | /api/HmiWidgetTemplate | Admin | 新建 |
| PUT | /api/HmiWidgetTemplate | Admin | 更新 |
| DELETE | /api/HmiWidgetTemplate/{id} | Admin | 删除（IsSystem 拒绝） |
| POST | /api/HmiWidgetTemplate/import | Admin | 导入（单） |
| POST | /api/HmiWidgetTemplate/import-bundle | Admin | 导入（批量） |
| GET | /api/HmiWidgetTemplate/{id}/export | 登录 | 导出（单，附件下载） |
| POST | /api/HmiWidgetTemplate/export-bundle | 登录 | 导出（批量，附件下载） |
