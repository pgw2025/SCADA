# 03 · 执行计划

> **8 个阶段、21 个步骤**。每步含四要素：任务 / 涉及文件 / 具体改动 / 验收标准。
> 路径约定：`Server/…` 指 `d:\CSharp\SCADA\Server`，`Client/…` 指 `d:\CSharp\SCADA\Client`。
> 并行建议：阶段一 ∥ 阶段二 ∥ 阶段五（互不依赖，可并行开发）；阶段三为核心集成，依赖一、二；六、七依赖后端；八收尾。

---

## 阶段一：数据层——授权实体与仓储

### 步骤 1：新增授权实体

| 项 | 内容 |
|---|---|
| 任务 | 创建 `ScadaProjectAuthorization` 实体类 |
| 涉及文件 | `Server/ScadaServer.Domain/Entities/ScadaProjectAuthorization.cs`（新增） |
| 具体改动 | 按 [02-详细设计 §1.2](02-详细设计.md#12-实体定义) 完整落地：`[Table("ScadaProjectAuthorizations")]`、复合主键三字段（`ProjectId`/`UserId`/`GrantedAt`）、XML 文档注释风格与 `ScadaProject.cs` 一致 |
| 验收标准 | `dotnet build ScadaServer.Domain` 通过；实体无导航属性、无自增列 |

### 步骤 2：EF 配置与迁移

| 项 | 内容 |
|---|---|
| 任务 | 注册 DbSet、OnModelCreating 配置、生成并应用数据库迁移 |
| 涉及文件 | `Server/ScadaServer.Infrastructure/Persistence/ScadaDbContext.cs`（修改）；`Server/ScadaServer.Infrastructure/Migrations/*_AddScadaProjectAuthorization.cs`（生成） |
| 具体改动 | ① DbSet 区追加 `ScadaProjectAuthorizations`（与其他 DbSet 同风格，L59-L70 区域）；② `OnModelCreating` 按 [02-详细设计 §1.3](02-详细设计.md#13-ef-配置scadadbcontext) 配置复合主键、双 FK（Cascade、约束名 `FK_ScadaProjectAuthorizations_*`）、`ix_scadaprojectauthorizations_userid` 索引；③ 执行迁移命令（见 [02-详细设计 §1.4](02-详细设计.md#14-迁移)） |
| 验收标准 | 迁移文件仅含 `CreateTable`（无既有表变更）；`database update` 后 MySQL 中可查到新表、复合主键与 2 个 FK；重复执行 update 幂等 |

### 步骤 3：新增仓储接口

| 项 | 内容 |
|---|---|
| 任务 | 定义 `IScadaProjectAuthorizationRepository` |
| 涉及文件 | `Server/ScadaServer.Domain/Interfaces/Repositories/IEntityRepositories.cs`（追加，建议紧跟 `IScadaProjectRepository` 定义之后保持领域聚合） |
| 具体改动 | 追加 [02-详细设计 §2.1](02-详细设计.md#21-接口追加到-ientityrepositoriescs) 的接口（4 方法：`GetProjectIdsByUserIdAsync` / `IsAuthorizedAsync` / `GetByProjectIdAsync` / `ReplaceForProjectAsync`）；接口注释明确「复合主键实体，不继承 IRepository<T,K>」 |
| 验收标准 | Domain 层编译通过；接口无 EF Core 依赖（仅 Domain 实体类型） |

### 步骤 4：仓储实现与 DI 注册

| 项 | 内容 |
|---|---|
| 任务 | 实现授权仓储并注册到 DI 容器 |
| 涉及文件 | `Server/ScadaServer.Infrastructure/Repositories/ScadaProjectAuthorizationRepository.cs`（新增）；`Server/ScadaServer.WebApi/Extensions/Database.Extensions.cs`（修改，仓储注册处） |
| 具体改动 | ① 实现类**直接注入 `ScadaDbContext`，不继承 `RepositoryBase`**（复合主键无单列 TKey，见 01 文档 P3-1）；② 四方法语义按 [02-详细设计 §2.2](02-详细设计.md#22-实现scadaprojectinfrastructurerepositories-新文件)，`ReplaceForProjectAsync` 只做 RemoveRange+AddRange，**不调用 SaveChanges**；③ DI：`services.AddScoped<IScadaProjectAuthorizationRepository, ScadaProjectAuthorizationRepository>()` |
| 验收标准 | 全解决方案编译通过；用 Swagger 或临时端点手工验证：插入一条授权 → `GetProjectIdsByUserIdAsync` 能查到 → `ReplaceForProjectAsync` 换集合后旧记录消失 |

---

## 阶段二：当前用户服务

### 步骤 5：定义 ICurrentUser 接口

| 项 | 内容 |
|---|---|
| 任务 | 在 Application 层定义当前用户抽象 |
| 涉及文件 | `Server/ScadaServer.Application/Interfaces/ICurrentUser.cs`（新增） |
| 具体改动 | 按 [02-详细设计 §3.1](02-详细设计.md#31-接口scadaserverapplicationinterfacesicurrentusercs-新文件)：三只读属性 `UserId`/`Username`/`IsAdmin`；注释说明「后台任务/测试环境无 HttpContext 时返回默认值」 |
| 验收标准 | Application 层编译通过；接口不引用任何 ASP.NET Core 类型 |

### 步骤 6：CurrentUser 实现 + DI 注册

| 项 | 内容 |
|---|---|
| 任务 | WebApi 层实现 JWT claims 解析并注册 |
| 涉及文件 | `Server/ScadaServer.WebApi/Services/CurrentUser.cs`（新增）；`Server/ScadaServer.WebApi/Extensions/Application.Extensions.cs`（修改，L64 `IScadaProjectAppService` 注册附近） |
| 具体改动 | ① 实现类注入 `IHttpContextAccessor`（已注册，审计服务在用）；解析规则见 [02-详细设计 §3.2](02-详细设计.md#32-实现scadaserverwebapiservicescurrentusercs-新文件)：`UserId`←claim `"id"`（TryParse 失败置 0）、`IsAdmin`←claim `"role"` 或 `ClaimTypes.Role` 比对 `SystemRoles.Admin`、`Username`←claim `"username"` 回退 `ClaimTypes.Name`；**HttpContext 为 null 时返回默认值**（P3-2）；② DI 注册 `AddScoped<ICurrentUser, CurrentUser>()` |
| 验收标准 | 编译通过；带 Admin token 请求任意端点断点观察 `IsAdmin=true`；带 Operator token 观察 `IsAdmin=false`、`UserId` 为对应用户 Id；无 token 场景由 FallbackPolicy 在到达服务前拦截，不触发解析 |

---

## 阶段三：后端授权核心（集成关键路径）

> 本阶段完成后，授权的「可见性」与「打开校验」即端到端生效（此时还没有授权管理入口，可先手工 SQL 插入授权记录验证）。

### 步骤 7：DTO 与服务接口扩展

| 项 | 内容 |
|---|---|
| 任务 | 定义授权 DTO；扩展 `IScadaProjectAppService` 接口 |
| 涉及文件 | `Server/ScadaServer.Application/DTOs/ScadaProjectAuthorizationDto.cs`（新增，含两个 DTO 类）；`Server/ScadaServer.Application/Interfaces/IScadaProjectAppService.cs`（修改） |
| 具体改动 | ① DTO 按 [02-详细设计 §4.5](02-详细设计.md#45-dtoscadaserverapplicationdtos-新文件)：`ScadaProjectAuthorizedUserDto`（5 字段）+ `SaveScadaProjectAuthorizationDto`（`UserIds` 带 `[Required]`）；② `IScadaProjectAppService` 追加两方法签名：`GetAuthorizedUsersAsync(int projectId)`（返回可空 List，null=工程不存在）、`SaveAuthorizationsAsync(int projectId, List<int> userIds)`（返回 bool，false=工程不存在；无效用户抛 ArgumentException） |
| 验收标准 | Application 层编译通过 |

### 步骤 8：ScadaProjectAppService 依赖注入 + 列表过滤

| 项 | 内容 |
|---|---|
| 任务 | 注入三个新依赖；实现 `GetListAsync` 按用户过滤 |
| 涉及文件 | `Server/ScadaServer.Application/Services/ScadaProjectAppService.cs`（修改） |
| 具体改动 | ① 构造函数与字段追加：`IScadaProjectAuthorizationRepository _authRepository`、`ICurrentUser _currentUser`、`ISystemUserRepository _userRepository`；② 新增私有谓词 `CanAccessAsync(int projectId)`（Admin 恒 true，否则查 `IsAuthorizedAsync`，见 [02-详细设计 §4.2](02-详细设计.md#42-私有授权判定核心谓词)）；③ `GetListAsync` 改造：`IsAdmin` → 现状返回全部；非 Admin → `GetProjectIdsByUserIdAsync(UserId)`，**`ids.Count == 0` 短路返回空列表**（P3-3），否则 `_repository.GetListAsync(p => ids.Contains(p.Id))` 后映射 DTO（复用现有 Select 投影写法） |
| 验收标准 | 编译通过；Swagger 手工验证：Admin token → 全量；Operator token（无授权记录）→ `[]`；SQL 手工插入授权记录后 → 仅授权工程 |

### 步骤 9：详情/整树授权校验

| 项 | 内容 |
|---|---|
| 任务 | `GetByIdAsync` / `GetTreeAsync` 增加访问校验（404 语义） |
| 涉及文件 | `Server/ScadaServer.Application/Services/ScadaProjectAppService.cs`（修改） |
| 具体改动 | ① `GetByIdAsync`：查到实体后、映射 DTO 前追加 `if (!await CanAccessAsync(id)) return null;`；② `GetTreeAsync`：`project == null` 与 `!await CanAccessAsync(id)` 合并判定返回 `null`（工程不存在与未授权统一 404，D3）。Controller 侧 `GetById`/`GetFullTree` 的 null → NotFound 分支已存在，**零改动**；`ExportAsync` 复用 `GetTreeAsync`，未授权同样返回 null（但导出后续在步骤 12 被 RequireAdmin 整体收紧，此路径对非 Admin 不再可达） |
| 验收标准 | Operator token：未授权工程 `GET {id}` / `{id}/full` → 404；已授权 → 200 数据完整；Admin 全部 200 |

### 步骤 10：授权读写方法实现

| 项 | 内容 |
|---|---|
| 任务 | 实现 `GetAuthorizedUsersAsync` / `SaveAuthorizationsAsync` |
| 涉及文件 | `Server/ScadaServer.Application/Services/ScadaProjectAppService.cs`（修改） |
| 具体改动 | 严格按 [02-详细设计 §4.4](02-详细设计.md#44-新增授权读写方法) 实现：<br>**Get**：工程不存在→null；`GetByProjectIdAsync` + 批量联查 SystemUsers（`GetListAsync(u => ids.Contains(u.Id))`）补全 Username/Role/Status，映射 DTO（`GrantedAt` 透传）。<br>**Save**：工程不存在→false；入参 `Distinct()` 去重 + 联查用户表**剔除 `Role == SystemRoles.Admin` 的用户**（D2）+ 校验存在性（不存在的 userId → `ArgumentException`，Controller 转 400）；`_uow.ExecuteInTransactionAsync` 内调用 `ReplaceForProjectAsync(projectId, 最终集合)`（事务提交负责 SaveChanges） |
| 验收标准 | 编译通过；单测式手工验证：Save 空数组 → 清空授权；Save 含 Admin userId → Admin 被剔除（Get 后无该条）；Save 含不存在 id → 400；同一工程二次 Save → 旧记录被替换（无重复行） |

---

## 阶段四：授权管理 API 与导出权限收紧

### 步骤 11：Controller 授权端点 + 审计

| 项 | 内容 |
|---|---|
| 任务 | 新增 GET/PUT authorizations 端点，写操作审计 |
| 涉及文件 | `Server/ScadaServer.WebApi/Controllers/ScadaProjectController.cs`（修改） |
| 具体改动 | ① `GET {projectId}/authorizations`：`[Authorize(Policy = "RequireAdmin")]`；调 `GetAuthorizedUsersAsync`，null → `NotFound()`，否则 `Ok(list)`；② `PUT {projectId}/authorizations`：`[Authorize(Policy = "RequireAdmin")]`；`[FromBody] SaveScadaProjectAuthorizationDto`；调 `SaveAuthorizationsAsync`，false → `NotFound()`；**保存前先 `GetAuthorizedUsersAsync` 取旧集合做 diff**，成功后 `AuditAsync("AUTHORIZE", projectId, "工程[id=x]名称「y」授权变更：+新增 a、b；-取消 c")`（空 diff 记「无变化」）；`ArgumentException` → `BadRequest(new { message })`（对齐 Import 错误处理）；③ XML 注释说明授权语义（未授权用户不可见、Admin 恒可见） |
| 验收标准 | Swagger：Admin → 200/204；Operator token → 403；不存在的工程 id → 404；含无效 userId 的 PUT → 400；`SystemLogs` 中可查到授权变更审计记录（含操作人与明细） |

### 步骤 12：工程导出收紧为 Admin

| 项 | 内容 |
|---|---|
| 任务 | `Export` 端点加 `RequireAdmin` |
| 涉及文件 | `Server/ScadaServer.WebApi/Controllers/ScadaProjectController.cs`（修改，L90 附近） |
| 具体改动 | `GET {id}/export` 追加 `[Authorize(Policy = "RequireAdmin")]`（D4：授权语义=「可见可打开」，导出=可迁移副本，Admin 专属；与 Import 对称）。Controller 内无需其他改动（服务层校验已在步骤 9 覆盖 Admin 路径不受影响） |
| 验收标准 | Admin token → 200 文件下载；Operator（即使已授权该工程）→ 403 |

---

## 阶段五：旁路封堵（可与阶段一并行先行）

### 步骤 13：ScadaPage 读取与画面导出收紧

| 项 | 内容 |
|---|---|
| 任务 | 页面读取端点 + 画面导出端点加 `RequireAdmin` |
| 涉及文件 | `Server/ScadaServer.WebApi/Controllers/ScadaPageController.cs`（修改） |
| 具体改动 | 四处追加 `[Authorize(Policy = "RequireAdmin")]`：① `GET`（`GetAll`，L38）；② `GET {id}`（L42）；③ `GET {id}/export`（L82，**P1-1 关键旁路**：`ExportPageAsync` 直查页面仓储不经 GetTreeAsync，必须以策略封堵）；④ 顺带确认 `POST import`（L98）已有（现状已存在，无需改）。其余写操作（Create/Update/Delete）现状已有该特性，逐一对齐确认即可 |
| 验收标准 | Operator token：`GET /api/ScadaPage?projectId=x` → 403；`GET /api/ScadaPage/1/export` → 403；Admin → 200；**前端组态编辑器（Admin 使用）功能不受影响** |

### 步骤 14：HmiComponent 读取收紧

| 项 | 内容 |
|---|---|
| 任务 | 组件读取端点加 `RequireAdmin` |
| 涉及文件 | `Server/ScadaServer.WebApi/Controllers/HmiComponentController.cs`（修改，L19-L22） |
| 具体改动 | `GET`（GetAll）与 `GET {id}`（GetById）两处追加 `[Authorize(Policy = "RequireAdmin")]`；写操作现状已有，确认即可 |
| 验收标准 | Operator token → 403；Admin → 200；编辑器组件树加载正常 |

---

## 阶段六：前端状态与缓存治理（防绕过）

### 步骤 15：resetScadaStore + 登出/登录接线

| 项 | 内容 |
|---|---|
| 任务 | 新增 `resetScadaStore()`；接入登出与登录流程 |
| 涉及文件 | `Client/src/store/scadaStore.ts`（修改）；`Client/src/api/authApi.ts`（修改，`performLogout` L107 / `performLogin` 成功分支） |
| 具体改动 | ① `scadaStore.ts` 按 [02-详细设计 §6.3](02-详细设计.md#63-状态与缓存治理scadastorets--authapits) 新增导出函数 `resetScadaStore()`：清空 `scadaProjects`/`projectSummaries`/`selectedProjectId`/`selectedPageId`，复位模块级标志 `_scadaInitialized`/`_summariesInitialized`；② `authApi.ts` 的 `performLogout()` 内调用（P1-2 修复核心）；③ `performLogin()` 成功分支同样调用（双保险，覆盖未登出直接换账号路径） |
| 验收标准 | 复现原漏洞场景并通过：Admin 登录（列表 3 个工程）→ 登出 → 同页签 Operator 登录 → 卡片页只显示该 Operator 被授权的工程（列表重新请求后端）；Network 面板确认重新发出了 `GET /api/ScadaProject` |

### 步骤 16：selectProject 非 Admin 强制回源

| 项 | 内容 |
|---|---|
| 任务 | 非 Admin 用户打开工程跳过本地缓存，始终回源校验 |
| 涉及文件 | `Client/src/store/scadaStore.ts`（修改，`selectProject` L144） |
| 具体改动 | 按 [02-详细设计 §6.3](02-详细设计.md#63-状态与缓存治理scadastorets--authapits)：`import { loginUser } from './userStore'` + `ROLE_ADMIN`（constants/roles）；`findProject` 命中分支仅对 Admin 生效（`const existing = loginUser.value?.role === ROLE_ADMIN ? findProject(rawId) : undefined`）。非 Admin 走既有 `loadProjectFull` 回源：后端 404 → 既有 catch → 返回 null → `ScadaPlayerView` 显示「工程加载失败或不存在」空态 |
| 验收标准 | Admin：编辑器/播放器间频繁切换工程仍走缓存（Network 无重复 full 请求）；Operator：撤销其授权后，在**未刷新的已打开标签页**内点击工程跳转 → 显示加载失败空态（回源 404 被拦截） |

---

## 阶段七：前端授权管理 UI

### 步骤 17：授权 API 封装

| 项 | 内容 |
|---|---|
| 任务 | `scadaApi.ts` 增加授权读写函数与类型 |
| 涉及文件 | `Client/src/api/scadaApi.ts`（修改） |
| 具体改动 | 按 [02-详细设计 §6.1](02-详细设计.md#61-api-封装clientsrcapiscadaapits-追加)：`ProjectAuthorizedUserDto` 接口 + `loadProjectAuthorizations` + `saveProjectAuthorizations`；注释标注与后端 DTO 的 camelCase 对齐关系（延续文件头部既有说明风格） |
| 验收标准 | `npm run build`（Client）通过；函数类型与步骤 7 的后端 DTO 字段一一对应 |

### 步骤 18：授权按钮与授权弹窗

| 项 | 内容 |
|---|---|
| 任务 | 工程卡片增加授权入口；实现用户多选授权弹窗 |
| 涉及文件 | `Client/src/components/ProjectPickerView.vue`（修改） |
| 具体改动 | ① **入口**：卡片头左上角（现有导出按钮旁）增加 `UserCheck` 图标按钮，`v-if="isAdmin"` + `@click.stop="openAuthModal(p)"`，title「管理授权」；② **弹窗状态**：`authModal = ref<{ visible, project: {id,name} \| null, users: SystemUser[], checkedIds: Set<number>, loading, saving }>`；③ **打开**：并行 `fetchSystemUsers()`（结果过滤 `role !== ROLE_ADMIN`，D2）与 `loadProjectAuthorizations(projectId)`（勾选态初始化）；④ **保存**：`saveProjectAuthorizations(id, [...checkedIds])` → 成功内联消息（复用现有 `importMessage` 容器，见现有导入导出交互）→ 关闭弹窗；失败展示 `err.response.data.message`；⑤ 弹窗 UI 按 [02-详细设计 §6.2](02-详细设计.md#62-授权弹窗projectpickerviewvue)：头部工程名 + 说明文案（「未列出的用户无法看到该工程；管理员默认可见全部」）+ 用户勾选列表（用户名/角色/状态徽标）+ 取消/保存按钮；modal 写法参考 `UserManagementView.vue` 既有遮罩层实现 |
| 验收标准 | Admin 打开弹窗：列表无 Admin 用户、已授权用户默认勾选；保存后重新打开勾选态正确；Operator 账号看不到授权按钮；保存成功后 Operator 账号列表出现/消失对应工程（换账号验证） |

### 步骤 19：空态文案区分

| 项 | 内容 |
|---|---|
| 任务 | 空列表文案按角色区分 |
| 涉及文件 | `Client/src/components/ProjectPickerView.vue`（修改，空状态区块 L148-L160） |
| 具体改动 | 按 [02-详细设计 §6.4](02-详细设计.md#64-空态文案区分projectpickerviewvue)：非 Admin 且无工程时主文案维持「暂无工程」、副文案改为「暂无可访问的工程，请联系管理员授权」；Admin 维持「请先在组态设计中创建并发布工程」；「去组态设计」按钮维持仅 Admin 可见（现状已如此，确认即可） |
| 验收标准 | 无授权 Operator 登录卡片页显示新文案；Admin 显示原文案 |

---

## 阶段八：验证与收尾

### 步骤 20：构建与迁移验证

| 项 | 内容 |
|---|---|
| 任务 | 全量构建 + 存量库迁移验证 + 既有测试回归 |
| 涉及文件 | —（命令执行） |
| 具体改动 | ① `dotnet build`（Server 全解决方案，零警告目标）；② 前端 `npm run build`（Client）；③ 在含存量数据的开发库执行 `database update`，确认迁移幂等、既有表零变更；④ 运行既有测试：`dotnet test ScadaServer.Infrastructure.Tests` 与 `dotnet test ScadaServer.Runtime.Tests`（本方案未触及 Runtime/采集链路，预期全绿；若有测试直调 `GET /api/ScadaPage`/`HmiComponent` 匿名读取，需按新策略补 Admin token——执行时逐一排查失败项） |
| 验收标准 | 三项命令全部通过；迁移脚本 review 确认无既有表 ALTER |

### 步骤 21：集成验证与回归测试

| 项 | 内容 |
|---|---|
| 任务 | 按 [04-测试与验收.md](04-测试与验收.md) 完整执行角色 × 场景矩阵验证与功能回归 |
| 涉及文件 | `doc/project-authorization/04-测试与验收.md`（执行清单，边测边勾） |
| 具体改动 | ① **授权功能**：三角色 × 列表可见性 / 打开校验 / 授权管理 / 撤销即时性 全矩阵；② **旁路封堵**：Operator 直调 Page/Component/Export API 全部 403；③ **前端缓存**：同页签账号切换、撤销后已打开标签页两场景；④ **回归**：组态编辑器（Admin）全功能、播放器（授权用户）、工程导入导出、用户管理；⑤ 验证过程中发现的缺陷回修后复测 |
| 验收标准 | 04 文档清单全绿；尤其 P1-1（旁路）、P1-2（缓存）两个原漏洞场景复测通过 |

---

## 附录：步骤 × 交付物速查表

| 步骤 | 层 | 新增文件 | 修改文件 | 解决的问题 |
|---|---|---|---|---|
| 1 | Domain | ScadaProjectAuthorization.cs | — | 授权数据模型 |
| 2 | Infrastructure | *_AddScadaProjectAuthorization.cs | ScadaDbContext.cs | 持久化 |
| 3 | Domain | — | IEntityRepositories.cs | 仓储契约 |
| 4 | Infrastructure | ScadaProjectAuthorizationRepository.cs | Database.Extensions.cs | 仓储实现 |
| 5 | Application | ICurrentUser.cs | — | 当前用户抽象 |
| 6 | WebApi | CurrentUser.cs | Application.Extensions.cs | JWT 解析 |
| 7 | Application | ScadaProjectAuthorizationDto.cs | IScadaProjectAppService.cs | 服务契约 |
| 8 | Application | — | ScadaProjectAppService.cs | **列表过滤** |
| 9 | Application | — | ScadaProjectAppService.cs | **打开校验（404）** |
| 10 | Application | — | ScadaProjectAppService.cs | 授权读写 |
| 11 | WebApi | — | ScadaProjectController.cs | 授权管理端点 + 审计 |
| 12 | WebApi | — | ScadaProjectController.cs | **导出语义（D4）** |
| 13 | WebApi | — | ScadaPageController.cs | **P1-1 旁路封堵** |
| 14 | WebApi | — | HmiComponentController.cs | **P1-1 旁路封堵** |
| 15 | Client | — | scadaStore.ts / authApi.ts | **P1-2 缓存绕过** |
| 16 | Client | — | scadaStore.ts | 撤销即时性（D7） |
| 17 | Client | — | scadaApi.ts | API 封装 |
| 18 | Client | — | ProjectPickerView.vue | 授权管理 UI |
| 19 | Client | — | ProjectPickerView.vue | 空态文案 |
| 20 | 全局 | — | — | 构建/迁移/测试验证 |
| 21 | 全局 | — | — | 集成与回归 |
