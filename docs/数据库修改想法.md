可以。结合我们前面确定的思路，我建议你第一版不要把数据库设计得过度复杂，但基础模型一定要把未来扩展空间留出来。

对于你的 ASP.NET Core + SqlSugar + S7/OPC UA 工业设备管理系统，我建议核心数据库先设计成 10 张表。

整体关系可以先看这张图：

┌──────────────────┐
│     Device       │
│     实体设备      │
│  1号输送带/搅拌机 │
└────────┬─────────┘
         │
         │ N:1
         ↓
┌──────────────────┐
│    Controller    │
│      控制器       │
│ S7-1500/OPC UA   │
└────────┬─────────┘
         │
         │ 1:N
         ↓
┌──────────────────┐
│ DeviceConnection │
│     通信连接      │
│ S7 / OPC UA      │
└──────────────────┘


┌──────────────────┐
│    DataModel     │
│     数据模型      │
│   输送带标准模型  │
└────────┬─────────┘
         │ 1:N
         ↓
┌──────────────────┐
│    DataPoint     │
│      数据点       │
│ Speed/Count/...  │
└────────┬─────────┘
         │
         │ 1:N
         ↓
┌──────────────────────┐
│  DataPointMapping    │
│       数据映射        │
│ DB10.DBD4 / OPC Node │
└──────────┬───────────┘
           │
           ↓
     DeviceConnection


Device
   │
   ↓
DeviceRuntime
   │
   ↓
RuntimeValue
   │
   ├── 实时值
   ├── Quality
   └── Timestamp


RuntimeValue
   │
   ↓
DataHistory
   历史数据

不过这里有一个地方我会稍微调整前面的设计：

> DeviceRuntime 和 RuntimeValue 不建议作为数据库表。



它们主要是内存运行时对象。

所以数据库真正需要的是下面这 10 张核心表。


---

一、10张表总览

#	表	用途	是否核心

1	Device	管理实体设备	⭐⭐⭐⭐⭐
2	Controller	管理 PLC / OPC UA Server	⭐⭐⭐⭐⭐
3	DeviceConnection	管理通信连接	⭐⭐⭐⭐⭐
4	DataModel	数据模型/设备模板	⭐⭐⭐⭐⭐
5	DataPoint	数据点定义	⭐⭐⭐⭐⭐
6	DataPointMapping	数据点与协议地址映射	⭐⭐⭐⭐⭐
7	DeviceDataModel	设备与数据模型绑定	⭐⭐⭐⭐
8	DeviceDataValue	最新值持久化快照	⭐⭐⭐
9	DataHistory	历史数据	⭐⭐⭐⭐
10	AlarmRecord	报警记录	⭐⭐⭐⭐


其中：

DeviceRuntime
RuntimeValue

不进入数据库。

它们属于你的 ASP.NET Core 运行时。


---

二、第一张表：Device

Device

这个表代表：

> 真实的工业设备/业务设备



例如：

1号输送带
2号输送带
1号搅拌机
1号包装机
1号机器人

而不是 PLC。


---

字段

字段	类型	说明

Id	bigint	主键
Code	varchar	设备编码
Name	varchar	设备名称
DeviceType	varchar	设备类型
Description	varchar	描述
ControllerId	bigint	所属控制器
ParentId	bigint	父设备
Location	varchar	安装位置
Enabled	bit	是否启用
Status	int	设备状态
Sort	int	排序
CreatedAt	datetime	创建时间
UpdatedAt	datetime	更新时间


例如：

Id	Code	Name	DeviceType	ControllerId

1	CONV001	1号输送带	Conveyor	1
2	CONV002	2号输送带	Conveyor	1
3	MIX001	1号搅拌机	Mixer	2



---

三、第二张表：Controller

Controller

这个表代表：

> 真正负责控制设备的控制器



例如：

S7-1200
S7-1500
Mitsubishi PLC
Omron PLC
OPC UA Server


---

字段

字段	类型	说明

Id	bigint	主键
Code	varchar	控制器编码
Name	varchar	控制器名称
ControllerType	varchar	PLC / OPCUA Server
Manufacturer	varchar	厂商
Model	varchar	型号
Description	varchar	描述
Enabled	bit	是否启用
CreatedAt	datetime	创建时间
UpdatedAt	datetime	更新时间


例如：

Id	Code	Name	ControllerType	Manufacturer	Model

1	PLC001	1号产线PLC	PLC	Siemens	S7-1500
2	OPC001	车间OPC服务器	OPCUA	Kepware	KEPServerEX



---

四、第三张表：DeviceConnection

这是通信层非常重要的一张表。

DeviceConnection

它代表：

> 系统如何连接 Controller



例如：

S7 TCP
OPC UA
Modbus TCP


---

字段

字段	类型	说明

Id	bigint	主键
ControllerId	bigint	控制器
Name	varchar	连接名称
Protocol	varchar	S7 / OPCUA
Host	varchar	IP/域名
Port	int	端口
ConfigJson	text	协议特殊配置
Enabled	bit	是否启用
Timeout	int	超时时间
ReconnectInterval	int	重连周期
CreatedAt	datetime	创建时间
UpdatedAt	datetime	更新时间


例如 S7：

Id	ControllerId	Protocol	Host	Port	ConfigJson

1	1	S7	192.168.1.10	102	{"rack":0,"slot":1}


OPC UA：

Id	ControllerId	Protocol	Host	Port	ConfigJson

2	2	OPCUA	192.168.1.20	4840	{"security":"None"}


这里建议：

Host
Port

放公共字段。

协议特殊参数放：

ConfigJson


---

五、第四张表：DataModel

DataModel

这个表代表：

> 一类设备的数据标准/模板



例如：

Conveyor.Standard
Mixer.Standard
Robot.Standard

例如：

输送带标准模型

里面规定：

RunStatus
Speed
Count
Alarm
Temperature


---

字段

字段	类型	说明

Id	bigint	主键
Code	varchar	模型编码
Name	varchar	模型名称
Version	varchar	版本
Description	varchar	描述
Enabled	bit	是否启用
IsPublished	bit	是否发布
CreatedAt	datetime	创建时间
UpdatedAt	datetime	更新时间


例如：

Id	Code	Name	Version	IsPublished

1	Conveyor.Standard	输送带标准模型	1.0	1
2	Mixer.Standard	搅拌机标准模型	1.0	1



---

六、第五张表：DataPoint

这是整个数据模型的核心。

DataPoint

它表示：

> 这个数据到底是什么



例如：

Speed
RunStatus
Count
Temperature
Alarm


---

字段

字段	类型	说明

Id	bigint	主键
DataModelId	bigint	所属模型
Code	varchar	数据点编码
Name	varchar	数据点名称
DataType	varchar	数据类型
AccessMode	varchar	Read / Write / ReadWrite
Unit	varchar	单位
Description	varchar	描述
Required	bit	是否必须
Enabled	bit	是否启用
Sort	int	排序
CreatedAt	datetime	创建时间
UpdatedAt	datetime	更新时间


例如：

Id	Model	Code	Name	DataType	AccessMode	Unit

1001	Conveyor	RunStatus	运行状态	Bool	Read	
1002	Conveyor	Speed	输送带速度	Float	Read	m/s
1003	Conveyor	Count	生产数量	Int32	Read	pcs
1004	Conveyor	Alarm	报警状态	Bool	Read	
1005	Conveyor	Temperature	电机温度	Float	Read	℃


注意：

这里没有 Address。

因为：

> DataPoint 只负责定义数据，不负责定义数据在哪里。




---

七、第六张表：DataPointMapping

这是和 DataPoint 配套的。

DataPointMapping

它表示：

> 这个 DataPoint 在某个具体设备上，实际从哪里读取。




---

字段

字段	类型	说明

Id	bigint	主键
DeviceId	bigint	实际设备
DataPointId	bigint	数据点
ConnectionId	bigint	使用哪个连接
Address	varchar	协议地址
RawDataType	varchar	原始数据类型
TransformConfig	text	转换配置
ScanInterval	int	采集周期
Enabled	bit	是否启用
CreatedAt	datetime	创建时间
UpdatedAt	datetime	更新时间


例如：

1号输送带

DeviceId = 1

DataPoint	Connection	Address	RawType

RunStatus	S7	DB10.DBX0.0	BOOL
Speed	S7	DB10.DBD4	REAL
Count	S7	DB10.DBD8	DINT
Alarm	S7	DB10.DBX12.0	BOOL
Temperature	S7	DB10.DBW14	INT



---

2号输送带

DeviceId = 2

DataPoint	Connection	Address	RawType

RunStatus	S7	DB20.DBX0.0	BOOL
Speed	S7	DB20.DBD4	REAL
Count	S7	DB20.DBD8	DINT
Alarm	S7	DB20.DBX12.0	BOOL
Temperature	S7	DB20.DBW14	INT


注意：

DataPoint
Speed

完全一样。

变化的是：

DataPointMapping


---

八、第七张表：DeviceDataModel

这一张表我建议保留。

为什么需要？

因为：

Device

和：

DataModel

实际上是：

> 多对多关系



例如：

1号输送带 → Conveyor.Standard
2号输送带 → Conveyor.Standard
3号输送带 → Conveyor.Standard

以后可能：

1号设备 → Conveyor.Standard V1
2号设备 → Conveyor.Standard V2

所以建立中间表。


---

DeviceDataModel

字段	类型	说明

Id	bigint	主键
DeviceId	bigint	设备
DataModelId	bigint	数据模型
Version	varchar	使用版本
IsPrimary	bit	是否主模型
Enabled	bit	是否启用
CreatedAt	datetime	创建时间


例如：

DeviceId	DataModelId	Version	IsPrimary

1	1	1.0	1
2	1	1.0	1
3	1	1.1	1



---

九、第八张表：DeviceDataValue

这张表和前面的 DeviceRuntime 要区分。

DeviceRuntime

是：

内存

实时运行。

而：

DeviceDataValue

是：

数据库

保存：

> 最后一次已持久化的值




---

字段

字段	类型	说明

Id	bigint	主键
DeviceId	bigint	设备
DataPointId	bigint	数据点
Value	text	当前值
DataType	varchar	类型
Quality	int	质量
Timestamp	datetime	时间
UpdatedAt	datetime	更新时间


例如：

Device	DataPoint	Value	Quality	Timestamp

1号输送带	Speed	1.52	Good	10:20:01
1号输送带	Count	1523	Good	10:20:01
1号输送带	RunStatus	true	Good	10:20:01
1号输送带	Temperature	38.5	Good	10:20:01


但是注意：

> 不要 PLC 每读取一次就 UPDATE 这个表。



例如：

500ms采集一次

一天就是：

172800 次

如果：

10000设备 × 20变量

数据量会非常恐怖。

所以：

PLC
 ↓
DeviceRuntime
 ↓
内存实时值
 ↓
批量持久化
 ↓
DeviceDataValue


---

十、第九张表：DataHistory

这个就是历史数据。

例如：

1号输送带 Speed

过去一天：

10:00 → 1.2
10:01 → 1.3
10:02 → 1.4
10:03 → 1.2
...


---

字段

字段	类型	说明

Id	bigint	主键
DeviceId	bigint	设备
DataPointId	bigint	数据点
Value	text	值
DataType	varchar	数据类型
Quality	int	数据质量
Timestamp	datetime	采集时间


例如：

DeviceId	DataPointId	Value	Quality	Timestamp

1	1002	1.25	0	10:00:00
1	1002	1.32	0	10:00:01
1	1002	1.35	0	10:00:02
1	1002	1.40	0	10:00:03



---

十一、第十张表：AlarmRecord

工业系统我强烈建议单独做报警表。

不要把报警直接塞进 DataHistory。

因为：

Alarm

本质上是一个事件。


---

AlarmRecord

字段	类型	说明

Id	bigint	主键
DeviceId	bigint	设备
DataPointId	bigint	相关数据点
AlarmCode	varchar	报警编码
AlarmName	varchar	报警名称
AlarmLevel	int	报警等级
Message	varchar	报警信息
StartTime	datetime	开始时间
EndTime	datetime	结束时间
Status	int	Active/Recovered
Value	text	触发时值
CreatedAt	datetime	创建时间


例如：

Device	AlarmCode	AlarmName	Level	StartTime	EndTime

1号输送带	E001	电机过热	2	10:20	10:25
1号输送带	E002	输送带堵料	3	11:05	11:08



---

十二、这10张表真正的关系

现在把它们全部串起来。

Controller
                         │
                         │ 1:N
                         ↓
                 DeviceConnection
                         │
                         │
                         │
Device ──────────────────┘
 │
 │
 ├────────────── DeviceDataModel
 │                     │
 │                     ↓
 │                 DataModel
 │                     │
 │                     │ 1:N
 │                     ↓
 │                 DataPoint
 │                     │
 │                     │ 1:N
 │                     ↓
 │              DataPointMapping
 │                     │
 └─────────────────────┘

运行时：

Device
   │
   ↓
DeviceRuntime
   │
   ├── DataPoint 1001 → RuntimeValue
   ├── DataPoint 1002 → RuntimeValue
   ├── DataPoint 1003 → RuntimeValue
   └── DataPoint 1004 → RuntimeValue

持久化：

RuntimeValue
      │
      ├──────────────→ DeviceDataValue
      │                  最新值
      │
      └──────────────→ DataHistory
                         历史值

报警：

RuntimeValue
      │
      ↓
 AlarmEngine
      │
      ↓
 AlarmRecord


---

十三、拿一台真实设备完整走一遍

假设现场有：

> 1号输送带



它由：

> S7-1500 PLC



控制。


---

Controller

Id = 1
Code = PLC001
Name = 1号产线PLC
Type = S7-1500


---

DeviceConnection

Id = 1
ControllerId = 1

Protocol = S7
Host = 192.168.1.10
Port = 102

ConfigJson:
{
    "rack": 0,
    "slot": 1
}


---

Device

Id = 1

Code = CONV001
Name = 1号输送带
DeviceType = Conveyor

ControllerId = 1


---

DataModel

Id = 1

Code = Conveyor.Standard
Name = 输送带标准模型
Version = 1.0


---

DataPoint

Id = 1001
Code = RunStatus
DataType = Bool

Id = 1002
Code = Speed
DataType = Float

Id = 1003
Code = Count
DataType = Int32


---

DeviceDataModel

DeviceId = 1
DataModelId = 1
Version = 1.0


---

DataPointMapping

DeviceId = 1
DataPointId = 1001
ConnectionId = 1

Address = DB10.DBX0.0
RawDataType = BOOL

DeviceId = 1
DataPointId = 1002
ConnectionId = 1

Address = DB10.DBD4
RawDataType = REAL

DeviceId = 1
DataPointId = 1003
ConnectionId = 1

Address = DB10.DBD8
RawDataType = DINT


---

十四、运行起来以后

PLC：

DB10.DBD4 = 1.25

驱动：

S7Driver

读取：

DB10.DBD4

得到：

1.25

通过：

DataPointMapping

找到：

DataPoint = Speed

然后：

DeviceRuntime

里面变成：

Device 1

Speed:
    Value = 1.25
    Quality = Good
    Timestamp = 2026-09-03 10:20:01

这时候前端通过 WebSocket：

DeviceRuntime
       ↓
SignalR/WebSocket
       ↓
前端

直接显示：

1号输送带
速度：1.25 m/s

而不是：

查询数据库

这对你的系统性能非常重要。


---

十五、SqlSugar实体最终可以这样开始

例如 DataPoint：

[SugarTable("data_point")]
public class DataPoint
{
    [SugarColumn(IsPrimaryKey = true, IsIdentity = true)]
    public long Id { get; set; }

    public long DataModelId { get; set; }

    public string Code { get; set; } = string.Empty;

    public string Name { get; set; } = string.Empty;

    public string DataType { get; set; } = string.Empty;

    public string AccessMode { get; set; } = string.Empty;

    public string? Unit { get; set; }

    public string? Description { get; set; }

    public bool Required { get; set; }

    public bool Enabled { get; set; }

    public int Sort { get; set; }

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}

DataPointMapping：

[SugarTable("data_point_mapping")]
public class DataPointMapping
{
    [SugarColumn(IsPrimaryKey = true, IsIdentity = true)]
    public long Id { get; set; }

    public long DeviceId { get; set; }

    public long DataPointId { get; set; }

    public long ConnectionId { get; set; }

    public string Address { get; set; } = string.Empty;

    public string? RawDataType { get; set; }

    public string? TransformConfig { get; set; }

    public int ScanInterval { get; set; }

    public bool Enabled { get; set; }

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}


---

十六、我建议你第一版数据库就按照这个思路

最终可以记成 4层：

第一层：设备资产

Device
Controller
DeviceConnection

解决：

> “我有什么设备？设备由谁控制？怎么连接？”




---

第二层：数据定义

DataModel
DataPoint
DeviceDataModel

解决：

> “这个设备有什么数据？每个数据是什么意思？”




---

第三层：数据映射

DataPointMapping

解决：

> “这个数据实际在 PLC/OPC UA 的什么位置？”




---

第四层：运行数据

数据库：

DeviceDataValue
DataHistory
AlarmRecord

内存：

DeviceRuntime
RuntimeValue

解决：

> “现在是多少？过去是多少？有没有报警？”




---

最终架构我建议你定成

┌───────────────┐
                 │    Device     │
                 │    实体设备     │
                 └───────┬───────┘
                         │
                         ↓
                 ┌───────────────┐
                 │  Controller   │
                 │  PLC/OPC UA   │
                 └───────┬───────┘
                         │
                         ↓
                 ┌───────────────┐
                 │DeviceConnection│
                 │   S7/OPC UA   │
                 └───────┬───────┘
                         │
                         │
      ┌──────────────────┘
      │
      ↓
┌───────────────┐
│   DataModel   │
│    数据模型     │
└───────┬───────┘
        │
        ↓
┌───────────────┐
│   DataPoint   │
│    数据点       │
└───────┬───────┘
        │
        ↓
┌───────────────────┐
│DataPointMapping   │
│  地址映射/转换      │
└─────────┬─────────┘
          │
          ↓
       PLC/OPC UA
          │
          ↓
   ┌──────────────┐
   │ DeviceRuntime│
   │    内存实时值  │
   └───────┬──────┘
           │
       ┌───┴────┐
       ↓        ↓
最新值快照     历史数据
       │        │
       ↓        ↓
DeviceDataValue DataHistory
           │
           ↓
       AlarmRecord

这套结构我认为非常适合你现在这个项目。

尤其是你之前规划的 DeviceWorker → DeviceRuntime → RuntimeManager → IProtocolDriver，可以非常自然地接到这里：

DeviceWorker
    ↓
DeviceConnection
    ↓
IProtocolDriver
    ├── S7Driver
    └── OpcUaDriver
          ↓
DataPointMapping
          ↓
DataPoint
          ↓
DeviceRuntime

下一步其实就到了一个非常关键的问题：这 10 张表如何设计成真正可落地的 SqlSugar 数据库，包括主键、外键、索引、唯一约束，以及 Device → Controller → Connection → DataModel → DataPoint → Mapping 到底应该怎么关联。 这个部分做好以后，你的数据库基本就可以直接开始建了。

可以，而且我建议你把“区域（Area）”作为独立的组织层级加入现在的模型中。

对于你这个工业设备管理系统，区域不应该只是 Device.Location 一个字符串，而应该成为真正的设备组织树。


---

一、我建议的整体关系

你现在的结构可以进一步变成：

区域 Area
   │
   ├── 区域
   │    ├── 区域
   │    │    ├── 设备
   │    │    └── 设备
   │    └── 设备
   │
   └── 设备

同时设备还有自己的控制关系：

Area
 │
 └── Device
       │
       └── Controller
              │
              └── DeviceConnection

例如一个真实工厂：

华东工厂
│
├── 一车间
│   │
│   ├── 原料区
│   │   ├── 原料输送机01
│   │   ├── 原料输送机02
│   │   └── 搅拌机01
│   │
│   └── 包装区
│       ├── 包装机01
│       └── 包装机02
│
└── 二车间
    │
    ├── 装配区
    │   ├── 装配机01
    │   └── 装配机02
    │
    └── 成品区
        └── 输送机01

这时候 Area 是“设备在哪里”，而 Controller 是：

> “设备由谁控制”。



这两个概念一定要分开。


---

二、增加 Area 表

我建议第一版设计成树形区域表。

Area

字段	类型	说明

Id	bigint	主键
ParentId	bigint?	父区域
Code	varchar	区域编码
Name	varchar	区域名称
AreaType	varchar	区域类型
Description	varchar	描述
Sort	int	排序
Enabled	bool	是否启用
CreatedAt	datetime	创建时间
UpdatedAt	datetime	更新时间


核心字段就是：

Id
ParentId
Code
Name
AreaType
Sort
Enabled

其中最重要的是：

ParentId


---

三、为什么使用 ParentId

例如：

Area
------------------------------------------------
Id    ParentId    Code        Name
1     NULL        FACTORY01   一号工厂
2     1           WORKSHOP01  一车间
3     2           RAW         原料区
4     2           PACK        包装区
5     1           WORKSHOP02  二车间
6     5           ASSEMBLY    装配区

实际上就是一棵树：

一号工厂
│
├── 一车间
│   │
│   ├── 原料区
│   │
│   └── 包装区
│
└── 二车间
    │
    └── 装配区

ParentId = NULL 表示根区域。


---

四、Device 怎么关联 Area？

我建议直接在 Device 增加：

AreaId

所以：

Device
├── Id
├── Code
├── Name
├── DeviceType
├── AreaId          ← 新增
├── ControllerId
├── ParentId
├── Enabled
└── ...

关系变成：

Area 1 ─────── N Device

例如：

Device	AreaId

原料输送机01	3
原料输送机02	3
搅拌机01	3
包装机01	4
包装机02	4
装配机01	6


这样你查询：

> 原料区所有设备



直接：

WHERE AreaId = 3


---

五、这样以后你的设备模型就很清晰了

我比较推荐你把几个概念严格区分：

Area
  ↓
“设备在哪里？”

Device
  ↓
“这是什么设备？”

Controller
  ↓
“谁控制它？”

DeviceConnection
  ↓
“通过什么通信方式连接？”

DataModel
  ↓
“这种设备有哪些标准数据？”

DataPoint
  ↓
“具体有哪些数据？”

DataPointMapping
  ↓
“这些数据在PLC哪里？”

DeviceRuntime
  ↓
“现在是什么状态？”

这个结构非常适合你后面做 SCADA。


---

六、Area 不建议放 Location 字符串

你之前的 Device 里面有：

Location

如果现在增加 Area，我建议：

删除或者弱化 Location 字段。

不要设计成：

Device
Location = "一车间/原料区/东侧"

因为这样以后非常难管理。

应该：

Device
AreaId = 3

然后：

Area 3
Name = 原料区
ParentId = 2

再向上：

原料区
  ↓
一车间
  ↓
一号工厂

系统可以自动得到完整路径：

一号工厂 / 一车间 / 原料区


---

七、AreaType 很有用

我建议保留：

AreaType

例如：

Factory
Workshop
ProductionLine
Area
Warehouse
Office

或者直接使用数字枚举：

public enum AreaType
{
    Factory = 1,
    Workshop = 2,
    ProductionLine = 3,
    Area = 4,
    Warehouse = 5
}

例如：

一号工厂       Factory
    ↓
一车间         Workshop
    ↓
生产线01       ProductionLine
    ↓
原料区         Area

这样前端可以根据类型做不同的显示。


---

八、甚至可以支持“生产线”

这个对于你的系统非常重要。

实际工业项目通常不是简单：

工厂
 └── 设备

而是：

工厂
│
├── 一车间
│   │
│   ├── 1号生产线
│   │   ├── 输送机01
│   │   ├── 输送机02
│   │   ├── 搅拌机01
│   │   └── 包装机01
│   │
│   └── 2号生产线
│       ├── 输送机03
│       └── 包装机02

所以 Area 表实际上可以承担：

> 工厂 → 车间 → 生产线 → 区域



这样的组织结构。


---

九、但这里有一个非常重要的问题

你现在 Device 已经有：

ParentId

这个字段和 Area.ParentId 不是一回事。

例如：

Area
一车间
 └── 1号生产线
      └── 包装区

这是物理/组织位置关系。

而：

Device
输送机01
 └── 输送机02

可能代表：

> 输送机02是输送机01的子设备。



这是设备层级关系。

所以我建议两个 ParentId 都保留，但含义必须明确：

Area.ParentId
    ↓
区域树

Device.ParentId
    ↓
设备树

不要混在一起。


---

十、最终数据库关系

加入 Area 后，你现在的模型可以变成：

┌──────────────┐
                 │     Area     │
                 │    区域表     │
                 └──────┬───────┘
                        │ 1:N
                        ↓
                 ┌──────────────┐
                 │    Device    │
                 │    设备表     │
                 └──────┬───────┘
                        │
                        │ N:1
                        ↓
                 ┌──────────────┐
                 │  Controller  │
                 │ PLC/OPC UA   │
                 └──────┬───────┘
                        │
                        │ 1:N
                        ↓
                 ┌────────────────┐
                 │DeviceConnection│
                 │ S7 / OPC UA    │
                 └────────────────┘

数据模型部分：

DataModel
    │
    ↓
DataPoint
    │
    ↓
DataPointMapping
    │
    ↓
Device

运行时：

Device
   ↓
DeviceWorker
   ↓
IProtocolDriver
   ↓
PLC / OPC UA
   ↓
DeviceRuntime
   ├── 实时值
   ├── 报警
   └── 历史数据


---

十一、我建议你的表最终变成 11 张

原来的 10 张：

1 Device
2 Controller
3 DeviceConnection
4 DataModel
5 DataPoint
6 DataPointMapping
7 DeviceDataModel
8 DeviceDataValue
9 DataHistory
10 AlarmRecord

现在增加：

11 Area

最终：

表	核心职责

Area	设备区域/组织树
Device	物理设备
Controller	PLC / OPC UA Server
DeviceConnection	通信连接
DataModel	设备数据模型
DataPoint	数据点定义
DataPointMapping	数据点地址映射
DeviceDataModel	设备与模型绑定
DeviceDataValue	最新数据
DataHistory	历史数据
AlarmRecord	报警记录


我认为这个结构已经比较接近一个真正可落地的工业设备管理平台了。


---

十二、SqlSugar 实体可以这样写

例如：

[SugarTable("Area")]
public class Area
{
    [SugarColumn(IsPrimaryKey = true, IsIdentity = true)]
    public long Id { get; set; }

    /// <summary>
    /// 父区域ID
    /// </summary>
    public long? ParentId { get; set; }

    /// <summary>
    /// 区域编码
    /// </summary>
    [SugarColumn(Length = 50)]
    public string Code { get; set; } = string.Empty;

    /// <summary>
    /// 区域名称
    /// </summary>
    [SugarColumn(Length = 100)]
    public string Name { get; set; } = string.Empty;

    /// <summary>
    /// 区域类型
    /// </summary>
    public int AreaType { get; set; }

    /// <summary>
    /// 描述
    /// </summary>
    [SugarColumn(Length = 500, IsNullable = true)]
    public string? Description { get; set; }

    /// <summary>
    /// 排序
    /// </summary>
    public int Sort { get; set; }

    /// <summary>
    /// 是否启用
    /// </summary>
    public bool Enabled { get; set; } = true;

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}

然后：

[SugarTable("Device")]
public class Device
{
    [SugarColumn(IsPrimaryKey = true, IsIdentity = true)]
    public long Id { get; set; }

    [SugarColumn(Length = 50)]
    public string Code { get; set; } = string.Empty;

    [SugarColumn(Length = 100)]
    public string Name { get; set; } = string.Empty;

    [SugarColumn(Length = 50)]
    public string DeviceType { get; set; } = string.Empty;

    /// <summary>
    /// 所属区域
    /// </summary>
    public long AreaId { get; set; }

    /// <summary>
    /// 所属控制器
    /// </summary>
    public long? ControllerId { get; set; }

    /// <summary>
    /// 父设备
    /// </summary>
    public long? ParentId { get; set; }

    public bool Enabled { get; set; } = true;

    public int Status { get; set; }

    public int Sort { get; set; }

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}


---

十三、还有一个我强烈建议你现在就考虑的东西

如果你的系统以后真的要做到：

1000 ~ 10000 台设备

那么 Area 不仅仅是为了显示树形菜单。

它以后可以成为很多功能的基础过滤条件：

用户权限
   ↓
只能查看 一号车间

或者：

报警
   ↓
一车间
   ↓
所有报警

或者：

统计
   ↓
一号生产线
   ↓
设备运行率
   ↓
设备 OEE

甚至：

Area
 ↓
Device
 ↓
Runtime
 ↓
Alarm
 ↓
History

因此我会建议你把 Area 设计成系统中的一级核心实体，而不是简单的 Location。

最终我比较推荐的核心结构

Area
                  │
           ┌──────┴──────┐
           ↓             ↓
        Area            Area
           │
           ↓
        Device
           │
     ┌─────┴─────┐
     ↓           ↓
Controller   DataModel
     │           │
Connection    DataPoint
                 │
          DataPointMapping
                 │
                 ↓
                PLC
                 │
                 ↓
           DeviceRuntime
          ┌──────┼──────┐
          ↓      ↓      ↓
        实时值   历史    报警

**下一步我建议直接把 Area + Device + Controller + DeviceConnection 四张表的完整 SqlSugar 实体、索引、唯一约束以及树形区域查询一起设计出来。**这样我们可以顺便把“一号工厂 → 一车间 → 1号产线 → 设备 → PLC”的实际数据库关系彻底定下来。