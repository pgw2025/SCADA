# 阶段 6：清理收尾与报警关联（2-3 天，中风险）

> 目标：在阶段 1-5 全部稳定运行（建议观察期 ≥ 1 周）后，删除兼容层与冗余，完成目标设计的最终形态收敛。**每一步都独立提交、独立可回滚；删除类操作放在最后且必须先做只读巡检。**
> 前置：阶段 1-5 全部完成并稳定运行。

---

## 步骤 6.1：只读巡检（删除前置门禁） ✅ 已完成（2026-09-03）

> 执行与留档：`docs/db-refactor/巡检SQL-阶段6-清理前置门禁.md`（含可复用门禁 SQL 与 5 项巡检结果全绿）。
> 键值快照基线：阶段 0 未留存 `_baseline/`，本轮补建三表 CSV（`docs/db-refactor/_baseline/`，已 .gitignore，不入库）。

**任务**：执行并留档以下巡检 SQL，**任何一项不通过则对应清理步骤顺延**：

1. 双读路径依赖统计：`SELECT COUNT(*) FROM Devices WHERE ConnectionId IS NULL;`（必须 = 0，或这些是刻意不采集的设备）
2. JsonConfig 与 Connection 一致性：抽样比对 `Devices.JsonConfig` 与其 `Connection.ConfigJson`（双写期结束后应一致；不一致的行先修复）
3. 主模型一致性：`Device.ModelId` ↔ `DeviceDataModels (IsPrimary=1)` 严格一致
4. AccessMode/IsReadOnly 一致性：`COUNT(LEFT JOIN ... WHERE 矛盾)` = 0
5. 键值快照第三次比对：Devices.Key / ModelVariables.Key 与阶段 0 基线零差异

**验收**：巡检报告全部通过。

## 步骤 6.2：运行时删除 JsonConfig 回退层 ✅ 已完成（2026-09-03）

**任务**：
1. [DeviceRuntime.cs](../../Server/ScadaServer.Runtime/Devices/DeviceRuntime.cs) `ConfigJson` 删除 `?? Device.JsonConfig` 回退分支（仅保留 Connection 路径）；
2. RuntimeManager 驱动键解析删除 Model.Protocol 回退分支；
3. [IRuntimeDevice.cs](../../Server/ScadaServer.Domain/Interfaces/IRuntimeDevice.cs) 注释更新。

**验收**：冒烟清单（S7/OPC UA/虚拟设备）全绿；确认无设备再依赖回退路径（步骤 6.1 第 1 项）。

> 实际验收（2026-09-03）：启动冒烟 OK——VIRTUAL 设备 PLC-DEV-7975 初始化 6 变量完成；S7 设备 PLC-DEV-3115（192.168.1.60 不可达）进入预期重连退避路径（日志 `已注册为待重连`），重连路径不依赖被删的 JsonConfig 回退分支。

## 步骤 6.3：应用服务停止双写 JsonConfig ✅ 已完成（2026-09-03）

**任务**：
1. [DeviceAppService.cs](../../Server/ScadaServer.Application/Services/DeviceAppService.cs)：创建/更新设备连接参数时不再写 `Device.JsonConfig`（只写 Connection）；
2. 设备 DTO/API 删除 jsonConfig 输出字段（前端已在阶段 3.6 改造完成）；
3. 前端 [deviceApi.ts](../../Client/src/api/deviceApi.ts) / DeviceManagementView 快速模式提交仍发送旧结构 → 后端接收后转换写入 Connection（**接收结构保留，存储去双写**）。

**验收**：改 IP → 热重载链路正常；`Devices.JsonConfig` 不再变化。

> 实现注记（2026-09-03）：`DeviceDto.ConfigJson` 为 PUT 接收结构不可删属性，故以
> `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` 抑制序列化写出 null——
> 输出侧字段名整体消失，反序列化不受影响（请求体仍可携带 configJson 原文入参）。
>
> 实际验收（2026-09-03，API 冒烟）：
> - GET `/api/device` 列表与 `/api/device/{id}` 详情 top-level 均不再含 `configJson` 字段；
> - PUT 改参（虚拟设备 1 `IntervalMs 1000→2000`）→ `DeviceConnections.ConfigJson` 更新、`Devices.JsonConfig` 保持不变、`Devices.Version` 1→2，日志触发 DeviceWorker stop→re-init 热重载循环；还原 PUT（2000→1000）同样通过；
> - 验收侧数据影响：设备 1 `Devices.Version` 因两次验收 PUT 自增至 3（键值/映射列未动）。

## 步骤 6.4：删除废弃列 ✅ 批次 B 已完成（2026-09-03）

> 批次 B 决策（2026-09-03，用户确认后执行）：本批先执行 6.6/6.4b/6.5/6.4a（见 6.7 批次 B 记录），废弃列全部删除完毕。

**任务**（每列独立迁移，顺序执行，任何一步出问题可停）：

### 6.4a：删除 `Devices.JsonConfig` + `Devices.Version` ✅ 已完成（2026-09-03）

- 决策：Version 仅服务于协议配置追踪（乐观锁/自增），语义迁移到 `DeviceConnections.UpdatedAt` 即可 → **两列均删**。
- 迁移 `20260903123446_DropDeviceJsonConfigAndVersion`：Up DropColumn 两列；Down 对称还原（JsonConfig longtext NULL / Version int default 1）。
- 代码清理：`Device` 实体删两属性；`DeviceAppService` 删连接投影兜底 `Connection?.ConfigJson ?? JsonConfig`、Create `Version=1`、Update 两处 `entity.Version++`（快速/高级模式）；`DatabaseInitializer.BackfillControllerAndConnectionAsync` 不再读 JsonConfig（遗留无连接设备补建空配置连接，维持"每设备必有连接"不变量）；`DeviceConnection.cs` XML cref 改纯文本避免 CS1574。
- 删列前核查：本地库 risky 行 = 0（无 `ConnectionId IS NULL` 且 JsonConfig 有值设备），无数据丢失；迁移前全量备份 `docs/db-refactor/_backups/scada_full_20260903_203513.sql`。

> 实际验收（2026-09-03，API 冒烟）：Devices 剩 13 列无 JsonConfig/Version、2 行原样；GET 列表/详情 top-level 无 `configJson`/`version` 键；快速模式 PUT 配置往返（RandomValues true→false）→ `Connection.ConfigJson` 还原、`Connection.UpdatedAt` 刷新（12:37:26），服务日志无列相关异常。

### 6.4b：删除 `IsReadOnly` 双列 → `AccessModeOverride` 重构 ✅ 已完成（2026-09-03）

- 迁移 `20260903120640_DropIsReadOnlyAddAccessModeOverride`：手工重写为无损顺序（先加 `AccessModeOverride` string(16)? → 按旧 `IsReadOnlyOverride` 回填 Read/ReadWrite → 删旧列），库验证 7/7 行无丢失、回填 4 ReadWrite + 3 NULL。
- 模板 `DataPoint` 仅留 `AccessMode`（Read/Write/ReadWrite，唯一权威）；实例 `DataPointMapping` 删 `IsReadOnlyOverride` bool? → `AccessModeOverride` string(16)?（null=继承模板），有效模式 = override ?? template；运行时只读判定统一 `EffectiveAccessMode == "Read"`。
- 后端 commit `49e5024`（15 文件）+ 前端 commit `d575b0e`（8 文件）；CSV 导出移除 IsReadOnly 列、导入保留 legacy 兼容解析。
- 验证：tsc exit 0 / vite build 21.65s / API 冒烟（DataPoint 7 条无 isReadOnly、mapping 6 条纯 AccessMode 语义、Device 详情输出 accessModeOverride/templateAccessMode/effectiveAccessMode）。

> 说明：任务清单中第 3 项 `DataModels.ProtocolId` 评估结论——维持现状（可空化语义弱化收益低，避免影响 DataModelView 创建流程），不执行。

## 步骤 6.5：报警关联数据点（AlarmRecord.DataPointId） ✅ 已完成（2026-09-03）

**任务**（落地为准目标设计的 `DataPointId` 命名，非原方案的 ModelVariableId）：
1. [AlarmRecord.cs](../../Server/ScadaServer.Domain/Entities/AlarmRecord.cs) 新增 `DataPointId`(int?)（置于 VariableKey 与 VariableName 之间）；
2. 迁移 `20260903122433_AddAlarmRecordDataPointId`：加可空列 + 回填（按设备主模型匹配 VariableKey：`JOIN Devices d → JOIN DataPoints dp ON dp.ModelId=d.ModelId AND dp.Key=ar.VariableKey`，匹配不上保持 NULL）；
3. 运行时写入链路带出 DataPointId：`DeviceWorker.FireEvent` 构造 `AlarmEvent` 填 `vr.Instance?.DataPointId ?? vr.Definition.Id`；`AlarmRecorder.MapToEntity` 落库；
4. 报警查询 API/DTO 输出 `dataPointId`（现有 VariableName 字段冗余保留）。

**验收**：存量回填 3/3、不一致 0；运行时写越限触发新报警 id=4 `dataPointId=3` 非 NULL。

> 实现注记（2026-09-03）：回填目标表名已随 6.6 重命名为 DataPoints；`AlarmEvent`/`AlarmRecordDto` 同步新增 DataPointId 透传。

## 步骤 6.6：表重命名对齐目标设计（DataPoints / DataPointMappings） ✅ 已完成（2026-09-03）

**任务**（用户确认执行——命名对齐目标设计文档，正式收敛 ModelVariable/DeviceVariable 术语）：
- `RenameTable` 迁移：`ModelVariables → DataPoints`、`DeviceVariables → DataPointMappings`，同步实体 `[Table]`、DbContext、DTO/仓储/服务/前端类型全链路命名（后端 commit `676d5fc` + 前端 commit `8768ae5`）。
- 影响评估：纯命名改动、无数据迁移，但触碰文件多、diff 大（~20+ 文件）；以 00-总览 §七名词对照表作为长期映射。

**验收**：`dotnet build` / tsc / vite build 全绿；API 冒烟 DataPoint 7 条 / DataPointMapping 6 条正常。

## 步骤 6.7：收尾 ✅ 批次 A + 批次 B 全部完成（2026-09-03）

**任务**：更新本方案文档各阶段"已完成"标记；合并分支，tag `db-refactor-phase6`。

> 批次 A（6.1/6.2/6.3）按 SCC 分组提交于 `feature/db-refactor`，打 tag `db-refactor-phase6`（当时指向批次 A 终点 eda67a8）。
>
> 批次 B 执行记录（2026-09-03，用户确认范围后同批执行，提交链见下；迁移全部落库，tag 已强制前移至批次 B 终点）：
>
> ```
> 949bd5f refactor(model): 阶段6.4a 删除 Devices.JsonConfig/Version 历史列(6.4a)
> d60e213 feat(alarm):    阶段6.5 报警记录关联数据点 AlarmRecord.DataPointId(6.5)
> d575b0e refactor(client): 阶段6.4b 权限字段切换 AccessMode 语义(6.4b前端)
> 49e5024 refactor(model): 阶段6.4b IsReadOnly 双列删除与 AccessModeOverride 重构(6.4b后端)
> 8768ae5 refactor(client): 6.6 前端类型与 API 同步 DataPoint/DataPointMapping 命名(6.6前端)
> 676d5fc refactor(model): 6.6 表重命名对齐目标设计——DataPoint/DataPointMapping(6.6后端)
> ```
>
> 新增迁移 4 个：`20260903114336_RenameDataPointTables`(6.6) / `20260903120640_DropIsReadOnlyAddAccessModeOverride`(6.4b) / `20260903122433_AddAlarmRecordDataPointId`(6.5) / `20260903123446_DropDeviceJsonConfigAndVersion`(6.4a)。
> 状态：分支 `feature/db-refactor` 未合并 main、未 push（待用户显式确认）。

---

## 回滚方案

| 步骤 | 回滚 |
|---|---|
| 6.2/6.3 | 恢复回退分支代码即可（数据仍在） |
| 6.4a | 迁移 `20260903123446_DropDeviceJsonConfigAndVersion` Down（JsonConfig longtext NULL / Version int default 1） |
| 6.4b | 迁移 `20260903120640_DropIsReadOnlyAddAccessModeOverride` Down（先加旧 bool 列再回填），须同步回退 49e5024/d575b0e 代码 |
| 6.5 | 迁移 `20260903122433_AddAlarmRecordDataPointId` Down（删列可空，应用回滚 d60e213 即可） |
| 6.6 | 迁移 `20260903114336_RenameDataPointTables` Down（RenameTable 反向），同步回退 676d5fc/8768ae5 |

## 风险清单

| 风险 | 等级 | 对策 |
|---|---|---|
| 删除回退层后暴露隐藏的旧数据路径 | 中 | 步骤 6.1 巡检门禁 + 观察期要求 |
| 删列迁移在备份未更新时执行 | 高 | **硬性要求：删除列迁移执行前重新 mysqldump 全量备份**（阶段 0.2 的备份已过时） |
| AccessModeOverride 改造引入写权限回归 | 中 | 独立 commit + 写值链路手测（可写变量写成功/只读变量被拒） |
