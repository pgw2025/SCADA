# 阶段 7 回归执行记录（沙箱首轮）

> 归属：db-refactor 阶段 7（09-阶段7-回归测试与上线.md）执行记录。
> 执行时间：2026-09-03 21:20-21:45（本地 `scada` 库，迁移链已全部落库；WebApi 冒烟于 http://localhost:5555，admin/123456）。
> 范围裁剪：按 09 文档 §一执行**沙箱内可自动化部分**；需真机/人工环境用例单列（见 §六），等待在目标环境执行。

---

## 一、1.1 数据库结构核查 ✅ 全绿

| # | 检查项 | 结果 |
|---|---|---|
| 1 | 目标表齐备 | 10 张 MySQL 表全存在（Areas/Devices/Controllers/DeviceConnections/DataModels/DataPoints/DataPointMappings/DeviceDataModels/AlarmRecords/**variablerealtime**——实表名单数）；DataHistory 无 MySQL 残留（在 InfluxDB）✅ |
| 2 | FK/索引抽查 | Devices FK4/KEY5、DataPoints FK1/KEY1、DataPointMappings FK3/KEY3、DeviceDataModels FK2/KEY3、DeviceConnections FK2/KEY2（SHOW CREATE TABLE 抽查）✅ |
| 3 | 键值快照 vs `_baseline` | Devices 2 行、DataPoints 7 行（原 ModelVariables）、DataPointMappings 7 行（原 DeviceVariables）与基线 CSV **锚点零差异** ✅（注：Devices.csv 的 HasJsonConfig/Version 两列随 6.4a 已删列，不再可比对） |
| 4 | 双写/覆盖一致性 | 主模型双写失同步 = 0；ConnectionId 覆盖率 2/2（100%）；每设备 IsPrimary 恰好 1 条 ✅ |

## 二、1.2 后端功能回归 ✅ 54/57（4 项 FAIL 均为缺陷或语义，见 §五）

| 模块 | 用例 | 结果 |
|---|---|---|
| 区域（9/9） | 树查询/子区域/增删改/防环(400)/非空删除保护(400)/子树设备过滤 | ✅ |
| 控制器（4/4） | CRUD/唯一 Code(400)/options | ✅ |
| 连接（4/4） | CRUD/按控制器列表/PUT configJson 更新 | ✅ |
| 数据模型（3/4） | CRUD/唯一 Code/唯一 Name | ⚠️ PUT 改名 500（**Bug#1**） |
| 数据点（7/7） | 创建 Read/ReadWrite/唯一 Key(400)/PUT AccessMode 三态/非法值(400)/导出→改→导入 Overwrite 往返 | ✅ |
| 设备快速模式（5/5） | 创建自动建 Controller+Connection/自动绑主模型/主模型数据点实例化映射/写值(可写 200、只读 400)/启停热重载 | ✅ |
| 映射覆盖语义（3/3） | override=Write→effective=Write；override=null→回退模板 Read | ✅ |
| 绑定与切主（4/4） | Bind 附加模型(落库)/SetPrimary 同步 Device.ModelId/主模型不可解绑(400)/Unbind 后清理 | ✅ |
| 附加模型映射（0/1） | 附加模型数据点手工实例化 | ❌ 400（**Bug#2**） |
| 设备高级模式（2/4） | attach 已有 controller+connection 创建/引用正确 | ⚠️ 删设备后共享连接/控制器被级联删除（**Bug#4**，MVP 下未见破坏） |
| 删除设备清理（4/4） | DELETE 后设备 404（204 语义）/快速模式专属连接与绑定清理（SQL 复查无孤儿） | ✅ |

## 三、1.3 沙箱内运行时回归 ✅ 8/9 + 1 缺陷

| # | 用例 | 结果 |
|---|---|---|
| 3 | 虚拟设备采集 | 设备1(PLC-DEV-7975) 6 变量实时出数 quality=Good、时间戳持续推进 ✅ |
| 4 | 写值链路 | 可写(sw=95)200；只读(kg1)400 ✅ |
| 7 | 报警链路 | 写 sw=95→AlarmRecord 新记录(id5/id6) dataPointId=3；写回 10→recoveredAt/recoveryValue 落库；ack 流转正常 ✅ |
| 8 | 实时快照 | variablerealtime 表与内存一致（sw=10 ↔ 10.0）✅ |
| 9 | 服务重启恢复 | 重启后 RuntimeManager 加载 2 设备（含「绑定 N 模型，主模型 X」日志）、设备1 恢复采集；**但 3 条历史遗留 active 报警不闭合（Bug#3）** ⚠️ |
| 10 | 设备状态推送 | 停用/启用状态正确（API 层）✅（SignalR 实时推送归人工清单） |

## 四、1.4 前端回归

- `npm run build`：**exit 0 通过**（首次因沙箱 dist 清空守卫拦截失败，按 memory 已知 quirk 用 `find dist -type f -delete` 规避后成功）。
- 浏览器交互类（五个视图全功能、控制台无新增报错）→ 人工清单。

## 五、回归发现的缺陷清单（修复状态：Bug#1-3 已修复、Bug#4 已按 D4-B 处置，见 11 文档 §九）

| # | 严重度 | 现象 | 根因（file:line） | 建议修复方向 |
|---|---|---|---|---|
| Bug#1 | P1 | 数据模型 PUT 编辑（改名等）返回 **500** | `DataModelRepository.GetByIdAsync` 用 `AsNoTracking().Include(Protocol)`（DataModelRepository.cs:29-35）返回游离实体；随后 `RepositoryBase.UpdateAsync`（RepositoryBase.cs:91-95）`Db.Set<T>().Update(entity)` graph-attach，与同请求 `ResolveProtocolIdAsync`→`_protocolRepository.GetByIdAsync`（tracking）已跟踪的 Protocol 同 key 冲突 → `InvalidOperationException`（DataModelAppService.cs:225） | 方案 A（最小）：GetByIdAsync 去掉 AsNoTracking（1 行，副作用：Update 时连带把 Protocol 标 Modified）；方案 B（基类正确化）：UpdateAsync 改 `Db.Entry(entity).State = EntityState.Modified`（detached 主实体，不 attach 导航），需全仓核查 UpdateRange/级联保存依赖 |
| Bug#2 | P1 | 附加绑定模型的数据点无法手工实例化为设备变量（400「不属于所绑定数据模型」），与 5.2 文档「解绑需先清理该模型下 DeviceVariable」语义不自洽 | `DataPointMappingAppService.cs:83` 只校验 `mv.ModelId != device.ModelId`（主模型），未查 `DeviceDataModels` 绑定表 | 校验改为：数据点 ModelId ∈ 设备绑定模型集合（`DeviceDataModels WHERE DeviceId`，含主/附加） |
| Bug#3 | P2 | 服务重启后，旧进程遗留的 active 报警记录（值已正常）永不闭合，只能靠再次越限→恢复触发（且只闭合最新一条） | `DeviceWorker.EvaluateRule` 的 `_ruleStates` 为**进程内内存状态机**（DeviceWorker.cs:551-608）；重启即丢；`AlarmRecorder.MarkRecoveredAsync` 只恢复「最新一条未恢复」（AlarmRecorder.cs:228-247），旧记录被永远跳过 | 启动时把 DB 中该设备 active 未恢复记录对应规则状态机预置为 active（下次值回落触发 Recovered）；或启动巡检对值已正常的遗留 active 直接补恢复事件 |
| Bug#4 | P3 | 高级模式（attach 已有连接，DTO 注释宣称「可多设备共享」）下删除设备会**级联删除被 attach 的共享 Controller/Connection**；若两设备真共享，删除其一将 FK 冲突或悬挂 | ~~`DeviceAppService.DeleteAsync` 未区分连接是否被其他设备引用~~【**勘误 2026-09-03 见 11 文档 §五**：经 EF 实证 + 代码走查，删除路径实为 `DeviceDeletionService` 且 `CleanupExclusiveControllerAndConnectionAsync` 带引用计数检查；Remove 游离图不删共享 reference 引用——疑似回归数据构造误判，处置改「真共享验证后关闭」】 | ~~删除前检查 Connection 引用计数 >0 则仅解除本设备引用~~【改：11 文档 §五 D4，推荐 D4-B = 真共享验证 + `DeviceDeletionService.cs:61` 顺带切 `GetByIdForUpdateAsync` 稳健化】 |

> 注：删除后 GET 返回 204（非 404）与 POST 映射重复实例化拒绝（主模型数据点已自动实例化）为脚本断言偏差，非缺陷。

### 5.1 修复状态（2026-09-03 阶段 7.5 批次，详见 11 文档 §七/§九）

| Bug# | 处置批次 | 状态 | 验证 |
|---|---|---|---|
| Bug#1 (P1, PUT 500) | 7.5-1 | ✅ 已修复 | `DataModel` 补 `GetByIdForUpdateAsync`（跟踪、无导航）+ Update 后重读返回；方案 C 落地 |
| Bug#2 (P1, 附加模型无法实例化) | 7.5-2 | ✅ 已修复 | 校验/加载改绑定模型集合（`GetBoundModelIdsAsync`，主/附加 ∪ ModelId 兜底） |
| Bug#3 (P2, 重启后遗留 active 不闭合) | 7.5-3 | ✅ 已修复 | 新增 `AlarmRecoveryStartupService` 启动巡检按实时值补恢复 + `MarkRecoveredAsync` 批量闭合同键全部未恢复 |
| Bug#4 (P3, 疑级联删共享) | 7.5-4 | ✅ 已处置 | D4-B：代码走查 + EF 实证确认现有引用计数逻辑正确（误判），`DeviceDeletionService.cs:61` 顺带切 `GetByIdForUpdateAsync` 防御稳健化 |

复测：`dotnet build` 0 错误 0 警告；`dotnet test` **96/96 通过**（既有 80 + 新增 16）。

## 六、需真实/人工环境执行清单（沙箱不可达）

| 类别 | 用例 | 环境要求 |
|---|---|---|
| §1.3-1/2 | S7 / OPC UA 设备连接 + 周期采集 | 可达的真机/仿真 PLC（本地 192.168.1.60 不可达） |
| §1.3-5 | 断线重连（拔网线→Offline→恢复→续采） | 真机 + 网络可控 |
| §1.3-6 | 历史数据连续性（**R1 红线**：InfluxDB 曲线无缝拼接 tag device_key+variable_key） | InfluxDB 运行环境 + 重构前历史 |
| §1.3-10 | 设备状态 SignalR 实时推送前端 | 浏览器 + 服务运行 |
| §1.4 | 五视图全功能 + LiveData/Dashboard/HMI/拓扑/历史/报警 + 浏览器控制台无报错 | 浏览器人工回归 |
| §1.5 | 自动化单测补充 | 见下 |

## 七、1.5 自动化测试评估

- **基线实测（2026-09-03 22:0x）**：`dotnet test ScadaServer.Infrastructure.Tests` —— **80/80 通过，0 失败 0 跳过**（净运行 448ms；11 项编译警告均为既有）。回归基线全绿，可作为 7.5 修复批次的对照基线。

现有 `ScadaServer.Infrastructure.Tests`（xunit，纯单测：地址序列化/S7 解析，不触 DB）。建议（成本低→高）：
1. **纯逻辑单测**（推荐先做）：6.4b 的 `NormalizeAccessModeOverride`/`ResolveAccessMode` 值域校验；阶段 6 映射 effective 解析回退链；报警条件 `MatchesCondition` 边界（含浮点容差）。
2. AppService 级测试：需仓储抽象/InMemory 或测试库，工程量大，可后置。

## 八、待用户决策项

> **2026-09-03 更新**：Bug#1-4 已完成源码根因核实 + EF 行为实证，细化方案与决策点（D1-D6）见 **`11-阶段7.5-修复方案.md`**（含 Bug#4 勘误、Bug#1 方案 C 推荐、Bug#2 连带缺陷、Bug#3 巡检方案）。下表仍列原始决策面，以 11 文档为准：

1. Bug#1-#3 修复方案选择（Bug#1 见方案 A/B；Bug#2/#3 方向如上）——是否启动修复？按「先方案后改码」约定，修复可独立为阶段 7.5 小批次（SCC 提交）。
2. Bug#4 是否随本期修（建议随共享连接遗留项）。
3. §六真实环境用例由谁在目标环境执行、何时执行。
4. §七单测是否补充（建议先做纯逻辑组）。
5. 上线策略（09 文档 §二）是否进入排期（本地 merge/push/tag 仍保持现状，待观察期）。
