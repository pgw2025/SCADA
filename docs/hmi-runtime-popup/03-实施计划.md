# 运行态弹窗（openPopup）实施计划

> 依据：[01-方案设计.md](./01-方案设计.md)（v1.1 修订版）+ [02-评审修订记录.md](./02-评审修订记录.md)
> 范围：**仅前端**（`Client/`），后端零改动；本计划为文档，实施时按步骤逐条执行。
> 总体：**5 个阶段、16 个步骤**；每步含【任务 / 改动文件 / 具体内容 / 验证 / 注意】五要素。
> 文中行号为编写时参考锚点，实施时以符号名（函数/常量）定位为准。

---

## 0. 总览

### 0.1 阶段-步骤总表

| 阶段 | 名称 | 步骤 | 产出 |
| --- | --- | --- | --- |
| 阶段 0 | 准备与基线 | 0.1 | 基线确认记录 |
| 阶段 1 | 类型与配置层 | 1.1–1.4 | 设计器可配置/可保存 openPopup 动作 |
| 阶段 2 | 运行时弹窗核心 | 2.1–2.6 | 播放器点击弹窗全链路（含订阅生命周期） |
| 阶段 3 | 预览宿主与既有缺口收口 | 3.1–3.3 | 编辑器预览可弹窗；画布面板跨设备绑定实时 |
| 阶段 4 | 回归验收与收尾 | 4.1–4.2 | 全量验收通过 + 文档收尾 |

依赖关系：阶段 1 → 阶段 2 → 阶段 3 → 阶段 4（串行）；阶段 2 内 2.1 先行（2.4 依赖收集器）。

### 0.2 关键设计决策（贯穿全部步骤）

| # | 决策 | 内容 |
| --- | --- | --- |
| D1 | 参数 schema | `openPopup { sourceComponentId: string; requireWritePermission?: boolean }`（页内组件引用制，非类型新建制） |
| D2 | 宿主接口 | `HmiEventDispatchContext.openPopup` 为**可选成员**，`runAction` 判空降级 `onBlocked` |
| D3 | 候选范围 | 事件面板下拉列出当前页**全部组件**（排除触发组件自身），命名沿用 `setPropTargetOptions` 先例 |
| D4 | 弹窗样式 | 对齐 `SetValueDialog` 约定：`fixed inset-0 z-[100] bg-black/50 backdrop-blur-sm`，无 teleport，挂 CanvasPanel 同级 |
| D5 | 设备收集 | 通用纯函数收集 `bindDeviceId` + props 中所有 `*DeviceId` 后缀数字字段（覆盖 vfd-motor-panel 全部 11 个变量级引用，未来组件自动生效） |
| D6 | 渲染快照 | PopupHost 渲染浅拷贝 descriptor：强制 `visible:true / locked:false`、剥离 `layerId`（对齐项目弹窗快照约束先例） |
| D7 | 弹窗形态 | 单层弹窗 + 幂等重开（同 id 重复调用保持已开） |
| D8 | 关闭时机 | X 按钮 / ESC / 点遮罩 三合一 + 切页自动关闭（`watch(currentPage.id)`） |
| D9 | 权限 | 默认"可看不可动"；`requireWritePermission` 同步实现、默认 false；后端 `[Authorize(Roles)]` 兜底不动 |

### 0.3 涉及文件全景

| 文件 | 操作 | 阶段 |
| --- | --- | --- |
| `Client/src/types.ts` | 修改（~10 行） | 1 |
| `Client/src/components/EventPanel.vue` | 修改（~50 行） | 1 |
| `Client/src/utils/componentDeviceRefs.ts` | **新增**（~25 行） | 2 |
| `Client/src/services/hmiEventService.ts` | 修改（~30 行） | 2 |
| `Client/src/components/PopupHost.vue` | **新增**（~170 行） | 2 |
| `Client/src/components/ScadaPlayerView.vue` | 修改（~60 行） | 2 |
| `Client/src/components/ScadaTopologyView.vue` | 修改（~40 行） | 3 |
| `docs/hmi-runtime-popup/*` | 更新 | 4 |

---

## 阶段 0：准备与基线（1 步）

### 步骤 0.1 基线确认与锚点校准

**【任务】** 冻结实施前状态，校准本计划引用的代码锚点，准备测试工程样例。

**【改动文件】** 无代码改动（仅记录）。

**【具体内容】**
1. 构建基线：`Client` 目录执行 `npm run build`，确认零错误（记录输出摘要，供阶段 4 对比）；
2. 锚点校准（本计划行号漂移时按符号定位）：
   - `types.ts`：`HmiEventActionKind`（约 L135）、`HmiEventAction.params`（约 L141）；
   - `hmiEventService.ts`：`HmiEventDispatchContext`（约 L24）、`runAction`（约 L69）；
   - `EventPanel.vue`：`ACTION_KIND_META`（约 L195）、`addAction`（约 L123）、动作参数模板区（约 L420 起）、底部提示（约 L545）；
   - `ScadaPlayerView.vue`：`boundDeviceIds`（约 L92）、`canControlWrite`（约 L187）、`eventCtx`（约 L309）、模板弹窗挂载区（约 L379，SetValueDialog 处）；
   - `ScadaTopologyView.vue`：`previewEventCtx`（约 L854）、EventPanel 挂载（约 L1790）；
3. 测试工程样例（用于阶段 2/3 联调，纯组态配置不改代码）：
   - 新建/选用测试工程，页面 P1 放置：1 个 `motor` 图标（绑定设备 A 运行变量）+ 1 块 `vfd-motor-panel`（`bindDeviceId`=设备 A，其中 `spFreqVar`/`currentVar` 至少一个变量级 `xxxDeviceId` 指向**设备 B**，用于验证跨设备订阅收口）；
   - 面板置于隐藏图层（图层 visible=false），命名如「1#泵变频面板」；
   - 准备 Operator、Admin、Viewer 三种测试账号。

**【验证】** build 通过；样例页面在编辑器正常显示；三种账号可登录。

**【注意】** 若 build 本身有既有告警，记录但不处理（不扩大范围）。

---

## 阶段 1：类型与配置层（4 步）

> 目标：设计器里能给任意组件配置 `click → openPopup{sourceComponentId}`，保存重载不丢失。
> 本阶段完成后弹窗尚不能打开（运行端未实现，属预期）。

### 步骤 1.1 `types.ts`：新增动作类型与参数字段

**【任务】** 扩展事件动作类型系统。

**【改动文件】** `Client/src/types.ts`

**【具体内容】**
1. `HmiEventActionKind` 联合类型追加成员：

```ts
export type HmiEventActionKind =
  | 'writeVar'
  | 'navigate'
  | 'runScript'
  | 'setProp'
  | 'openPopup';   // 新增：打开弹窗面板（运行态弹出页内组件为模态窗口）
```

2. `HmiEventAction.params` 追加两个字段（注释风格与既有一致）：

```ts
    /** openPopup：弹窗内容源组件（当前页面内的组件 id，运行态以模态弹出） */
    sourceComponentId?: string;
    /** openPopup：开窗是否要求写权限（true=非 Operator/Admin 拦截开窗；默认 false 可看不可动） */
    requireWritePermission?: boolean;
```

**【验证】** `npm run build`（或 `vue-tsc`）通过——此时尚无消费方，纯类型扩展。

**【注意】** 可选字段 + 联合类型追加均为向后兼容（旧数据无 `openPopup` 动作不受影响）。

### 步骤 1.2 `EventPanel.vue`（脚本）：动作元数据与初始化

**【任务】** 让事件面板"认识" openPopup 动作。

**【改动文件】** `Client/src/components/EventPanel.vue`

**【具体内容】**
1. `ACTION_KIND_META`（约 L195）追加（lucide 图标导入处补 `AppWindow`）：

```ts
const ACTION_KIND_META: Record<HmiEventActionKind, { label: string; icon: any }> = {
  writeVar: { label: '写变量', icon: PencilLine },
  navigate: { label: '页面跳转', icon: CornerUpRight },
  runScript: { label: '运行系统脚本', icon: FileCode2 },
  setProp: { label: '组件控制', icon: Settings2 },
  openPopup: { label: '打开弹窗面板', icon: AppWindow },   // 新增
};
```

2. `addAction`（约 L123）的 `params` 初始化三元链追加分支：

```ts
    params: kind === 'writeVar'
      ? { writeMode: 'setBit', deviceId: null, variableKey: '' }
      : kind === 'navigate'
        ? { targetPageId: '' }
        : kind === 'runScript'
          ? { scriptId: undefined }
          : kind === 'openPopup'                                   // 新增分支
            ? { sourceComponentId: '', requireWritePermission: false }
            : { targetComponentId: '', patch: {} },
```

3. 候选下拉数据源（新增 computed，模式照抄 `setPropTargetOptions` 约 L238）：

```ts
// ===== openPopup：弹窗源组件候选（当前页全部组件，排除自身） =====
// 命名沿用 setPropTargetOptions 先例；不限定面板类型——弹窗内容可为任意组件（看板/趋势等），
// 组件是否适合弹窗由设计者判断（trend-chart 等依赖画布数据通道的组件首版不保证数据，见方案 §8-E3）。
const popupSourceOptions = computed(() =>
  (props.pageComponents ?? [])
    .filter((c) => c.id !== props.selectedComponent?.id)
    .map((c) => ({ id: c.id, name: `${c.name || c.type} (${c.id.slice(-6)})` }))
);
```

**【验证】** 添加动作下拉出现「打开弹窗面板」项；选中后动作卡片 kind 正确、params 获得初始值。

**【注意】** `pageComponents` prop 已由 `ScadaTopologyView` 传入（约 L1790 `:page-components="currentPage.components"`），无需新增数据通道。

### 步骤 1.3 `EventPanel.vue`（模板）：openPopup 参数编辑区

**【任务】** 提供弹窗源组件选择与权限标志配置 UI。

**【改动文件】** `Client/src/components/EventPanel.vue`（模板动作参数区，setProp 模板之后）

**【具体内容】** 在 `v-else-if="action.kind === 'setProp'"` 模板块之后追加并列分支（结构/样式类复用既有参数行）：

```html
<!-- openPopup 参数 -->
<template v-else-if="action.kind === 'openPopup'">
  <div class="space-y-1.5">
    <label class="text-[10px] text-slate-500">弹窗内容（页面内组件）</label>
    <select :value="action.params.sourceComponentId ?? ''"
      @change="updateActionParams(activeEventType, action.id, { sourceComponentId: ($event.target as HTMLSelectElement).value })">
      <option value="">请选择组件…</option>
      <option v-for="t in popupSourceOptions" :key="t.id" :value="t.id">{{ t.name }}</option>
    </select>
    <label class="flex items-center gap-1.5 cursor-pointer" title="勾选后仅 Operator/Admin 可打开弹窗">
      <input type="checkbox" :checked="action.params.requireWritePermission === true"
        @change="updateActionParams(activeEventType, action.id, { requireWritePermission: ($event.target as HTMLInputElement).checked })" />
      <span class="text-[10px] text-slate-500">开窗需写权限（默认关闭=可看不可动）</span>
    </label>
  </div>
</template>
```

同时更新面板底部提示文案（约 L545）：在「写变量与脚本动作需 Operator/Admin 权限」之后补一句
「弹窗面板默认全员可看不可动；勾选开窗需写权限后仅操作员/管理员可打开」。

**【验证】** 选中 motor 图标 → 事件面板 click 事件 → 添加「打开弹窗面板」→ 下拉能列出步骤 0.1 配置的「1#泵变频面板」→ 保存 → 刷新/重新打开工程，配置回显完整。

**【注意】** 候选下拉只列**当前页**组件——E2 边界（删除组件后下拉自动消失）由此天然满足。

### 步骤 1.4 阶段验证：配置持久化链路

**【任务】** 端到端确认配置落库与回显。

**【具体内容】**
1. 配置一条 `click → openPopup{sourceComponentId=面板id}` 并保存工程；
2. 浏览器刷新重进编辑器，确认事件配置完整回显（`props.events` 随 `PropsJson` 落库机制自动生效，无需后端改动）；
3. 检查 `dotnet` 侧无需变更：事件配置在 `HmiComponent.PropsJson` 内透传（防御性确认，不改代码）；
4. 运行态点击电机 → **预期无反应**（openPopup 尚无运行端实现，`runAction` 无该分支直接 return——不报错）。

**【验证】** 上述 1/2/4 表现符合预期；`npm run build` 通过。

---

## 阶段 2：运行时弹窗核心（6 步）

> 目标：播放器内全链路可用——点击弹窗、实时值、权限、关闭、订阅生命周期。

### 步骤 2.1 新增设备引用收集器

**【任务】** 提供通用纯函数，收敛"一个组件引用了哪些设备"的判定（D5）。

**【改动文件】** `Client/src/utils/componentDeviceRefs.ts`（**新增**）

**【具体内容】**

```ts
import type { HMIComponent } from '../types';

/**
 * 收集组件引用的全部设备 id（SignalR 订阅用）。
 * - bindDeviceId：组件主绑定；
 * - props 中所有以 "DeviceId" 结尾且值为合法数字的字段（opDeviceId、startDeviceId、spFreqDeviceId…
 *   vfd-motor-panel 全部 11 个变量级引用；未来组件新增 xxxDeviceId 字段自动覆盖，无需维护白名单）。
 */
export const collectComponentDeviceRefs = (c: HMIComponent | null | undefined): Set<number> => {
  const ids = new Set<number>();
  if (!c) return ids;
  if (c.bindDeviceId != null) ids.add(Number(c.bindDeviceId));
  for (const [key, val] of Object.entries(c.props ?? {})) {
    if (!/DeviceId$/i.test(key)) continue;
    const n = Number(val);
    if (val !== '' && val !== null && val !== undefined && !Number.isNaN(n)) ids.add(n);
  }
  return ids;
};
```

**【验证】** 单元心智校验（可写临时断言或控制台验证后删除）：对步骤 0.1 样例面板（bindDeviceId=A、spFreqDeviceId=B）收集结果为 `{A, B}`。

**【注意】** 纯函数无副作用、无依赖，阶段 3 的 `boundDeviceIds` 收口将复用。

### 步骤 2.2 `hmiEventService.ts`：上下文接口与动作分支

**【任务】** 事件分发系统支持 openPopup（D2：可选成员 + 判空降级）。

**【改动文件】** `Client/src/services/hmiEventService.ts`

**【具体内容】**
1. `HmiEventDispatchContext`（约 L24）追加可选成员（置于 `applyRuntimePatch` 之后）：

```ts
  /** 打开弹窗回调（运行态/预览宿主实现；未实现的宿主由 runAction 降级提示） */
  openPopup?: (sourceComponentId: string) => void;
```

2. `runAction`（约 L69）的 switch 追加分支（置于 `setProp` 之后）：

```ts
    case 'openPopup': {
      // 可选：开窗前权限拦截（默认 false=可看不可动，仅面板内操作受权限约束）
      if (p.requireWritePermission && !ctx.canControlWrite) {
        ctx.onBlocked?.('当前角色无弹窗操作权限');
        return;
      }
      const sourceId = p.sourceComponentId;
      if (!sourceId) return;
      // 宿主能力判空降级（预览宿主未实现时不崩溃）
      if (!ctx.openPopup) {
        ctx.onBlocked?.('当前宿主不支持弹窗动作');
        return;
      }
      ctx.openPopup(sourceId);
      return;
    }
```

**【验证】** `npm run build` 通过（接口可选成员追加不影响 `ScadaTopologyView` 既有构造，编译零错误）。

**【注意】** 该分支位于 `dispatchComponentEvent` 动作循环内，动作链顺序执行语义与既有四类动作一致。

### 步骤 3 新增 `PopupHost.vue`：模态容器

**【任务】** 弹窗宿主组件（D4/D6/D7/D8 + 标题栏 O1）。

**【改动文件】** `Client/src/components/PopupHost.vue`（**新增**）

**【具体内容】** 组件契约与关键实现：

```vue
<script setup lang="ts">
import { computed, onMounted, onUnmounted } from 'vue';
import type { HMIComponent } from '../types';
import HMIWidget from './HMIWidget.vue';
import { X } from 'lucide-vue-next';

const props = defineProps<{
  /** 弹窗内容源组件（页内组件对象，宿主负责解析） */
  component: HMIComponent;
  /** 当前角色是否有写权限（用于控件锁标显示；面板自身 hasWritePermission 同步生效） */
  canControlWrite?: boolean;
}>();

const emit = defineEmits<{ (e: 'close'): void }>();

// 渲染快照：浅拷贝强制可见/未锁/无图层归属（对齐项目弹窗组件快照约束先例），
// 避免源组件隐藏状态与图层属性影响模态渲染，也隔离模态内交互对画布源对象的污染。
const descriptor = computed<HMIComponent>(() => ({
  ...props.component,
  visible: true,
  locked: false,
  layerId: undefined,
  props: { ...props.component.props },
}));

// ESC 关闭（D8 三合一之一）
const onKeydown = (e: KeyboardEvent) => { if (e.key === 'Escape') emit('close'); };
onMounted(() => window.addEventListener('keydown', onKeydown));
onUnmounted(() => window.removeEventListener('keydown', onKeydown));
</script>

<template>
  <!-- 对齐 SetValueDialog 既有约定：fixed inset-0 z-[100]，无 teleport -->
  <div class="fixed inset-0 z-[100] flex items-center justify-center bg-black/50 backdrop-blur-sm p-4"
    @click.self="emit('close')">
    <div class="w-[min(92vw,640px)] max-h-[88vh] rounded-xl shadow-2xl overflow-hidden flex flex-col
                bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700">
      <!-- 标题栏：源组件名（O1：多电机页面区分几号泵） -->
      <div class="flex items-center justify-between px-4 py-2.5 border-b border-slate-200 dark:border-slate-700 shrink-0">
        <span class="text-sm font-bold text-slate-800 dark:text-slate-100 truncate">
          {{ component.name || component.label || component.type }}
        </span>
        <button @click="emit('close')"
          class="p-1 rounded text-slate-400 hover:text-red-500 hover:bg-slate-100 dark:hover:bg-slate-800 cursor-pointer"
          title="关闭 (Esc)">
          <X class="w-4 h-4" />
        </button>
      </div>
      <!-- 内容区：复用 HMIWidget 渲染链（builtinRenderers 已注册 vfd-motor-panel）。
           value 传 0：面板组件不消费该 prop（自读 devices store）；
           control-locked 取反写权限：非授权时带绑定变量的控件显示既有锁标。 -->
      <div class="flex-1 overflow-auto p-2">
        <HMIWidget :component="descriptor" :value="0" :is-active-mode="true"
          :control-locked="!canControlWrite" />
      </div>
    </div>
  </div>
</template>
```

实现要点（照抄设计，实施时按需微调样式）：
- 内容容器 `w-[min(92vw,640px)]`：桌面 640px 封顶、移动 92vw 近全屏（双端自适应）；
- `@click.self` 点遮罩关闭（区分遮罩与面板本体点击，防止误关）；
- `descriptor` 为 computed：源组件 props 变化（如运行态补丁）自动跟随；
- 面板高度自适应：`vfd-motor-panel` 为 flex 布局组件，容器给足高度（`min(80vh, 520px)` 可按实机微调）。

**【验证】** 组件暂无法独立联调（宿主下一步接）；先 `npm run build` 确认编译与类型正确。

**【注意】** 不使用 teleport（D4）；z-[100] 与 SetValueDialog 同层——同屏仅一个弹窗（D7 单层），无层叠冲突。

### 步骤 2.4 `ScadaPlayerView.vue`：弹窗状态、事件接入与挂载

**【任务】** 播放器宿主集成：开/关函数 + `eventCtx` 注入 + PopupHost 挂载 + E2 悬空引用处理 + E5 幂等。

**【改动文件】** `Client/src/components/ScadaPlayerView.vue`

**【具体内容】**
1. import 区补：`import PopupHost from './PopupHost.vue';` 与 `import { collectComponentDeviceRefs } from '../utils/componentDeviceRefs';`
2. `canControlWrite`（约 L187）之后新增弹窗状态区：

```ts
// ===== 弹窗面板（openPopup 动作）：单层弹窗 + 幂等重开（方案 §5/§8-E5） =====
const activePopupId = ref<string | null>(null);
const activePopupComponent = computed(() =>
  activePopupId.value
    ? (currentPage.value?.components ?? []).find((c) => c.id === activePopupId.value) ?? null
    : null
);

const openPopup = (sourceComponentId: string) => {
  const target = (currentPage.value?.components ?? []).find((c) => c.id === sourceComponentId);
  // E2 悬空引用：面板已被删除 → 提示且不开窗（配置入口只列现存组件，此为运行期兜底）
  if (!target) {
    showToast('弹窗目标组件不存在（可能已被删除）', 'warning');
    addLog('组态运行', `弹窗打开失败：目标组件 [${sourceComponentId}] 不存在`, 'warning');
    return;
  }
  // E5 幂等：同 id 重复点击保持已开，不叠加
  activePopupId.value = sourceComponentId;
  addLog('组态运行', `打开弹窗面板: [${target.name || target.type}]`, 'normal');
};

const closePopup = () => { activePopupId.value = null; };

// E1 切页自动关闭：nav-menu 跳转后 sourceComponentId 跨页失效；退订由下方订阅 watch 自动完成
watch(() => currentPage.value?.id, () => { closePopup(); });
```

3. 弹窗生命周期设备订阅（差集式，置于上述代码之后；这是数据不 stale 的关键，方案 §6.3）：

```ts
// 弹窗设备订阅差集：打开时补订「收集集合内尚未订阅」的设备，关闭时退订「不再被引用」的设备。
// 注意与 boundDeviceIds 主订阅的交集：主集合已含 bindDeviceId（v-show 隐藏组件不参与过滤），
// 此处只做增量补订/退订，避免干扰主 watch 的对账逻辑。
watch(activePopupComponent, (comp, oldComp) => {
  const pageIds = boundDeviceIds.value; // 主订阅快照（只读，不修改）
  const oldIds = collectComponentDeviceRefs(oldComp);
  const newIds = collectComponentDeviceRefs(comp);
  // 补订：新集合中主订阅未覆盖的设备
  newIds.forEach((id) => { if (!pageIds.has(id)) subscribeDeviceTelemetry(id); });
  // 退订：旧集合中主订阅未覆盖、且新集合不再引用的设备
  oldIds.forEach((id) => {
    if (!pageIds.has(id) && !newIds.has(id)) unsubscribeDeviceTelemetry(id);
  });
});
```

4. `eventCtx`（约 L309）追加成员（与既有回调同风格）：

```ts
  openPopup,          // 弹窗动作回调（hmiEventService runAction 分支消费）
```

5. 模板挂载（约 L379，SetValueDialog 旁并列，CanvasPanel 容器**外**同级）：

```html
    <!-- openPopup 弹窗面板：复用 HMIWidget 渲染源组件快照（方案 §5） -->
    <PopupHost v-if="activePopupComponent" :component="activePopupComponent"
      :can-control-write="canControlWrite" @close="closePopup" />
```

**【验证】**
1. Operator 登录播放器 → 点击电机 → 弹窗出现，标题显示「1#泵变频面板」；
2. 面板数值实时刷新（设备 A/B 变量）；设备 B 专门验证：改 B 的变量值，弹窗 `spFreq`/电流跟随变化（收口前该值 stale）；
3. 重复点击电机 → 仍只有一个弹窗（幂等）；
4. X / ESC / 点遮罩三种方式关闭均生效。

**【注意】**
- PopupHost 必须在 CanvasPanel 的缩放容器**之外**（模板中 CanvasPanel 包在 `v-else` 的 `h-full w-full` div 内，PopupHost 放该 div 之后、与 SetValueDialog 同级）——否则随 adaptMode 缩放失真；
- `subscribeDeviceTelemetry/unsubscribeDeviceTelemetry` 已在文件 import（既有），无需新增；
- 组件卸载兜底：既有 `onUnmounted`（约 L136）遍历 `boundDeviceIds.value` 退订；弹窗补订设备若不在主集合，需在其后补一行 `collectComponentDeviceRefs(activePopupComponent.value).forEach(...)` 退订兜底（防播放器直接卸载残留订阅）。

### 步骤 2.5 边界行为完善

**【任务】** 收口三类边界（部分已在 2.4 内联，本步集中核验与补漏）。

**【具体内容】**
1. **E1 切页**：弹窗开着 → 点击 nav-menu 跳转其它画面 → 弹窗自动关闭；原页面订阅按 watch 退订（网络面板确认 unsubscribe 调用）；
2. **E2 悬空引用**：编辑器删除面板组件后保存 → 播放器点击电机 → toast「弹窗目标组件不存在」，不崩溃；
3. **权限三层核验**：
   - Viewer 登录：点击电机 → 弹窗正常打开（只读）；面板启/停/设频按钮**置灰**，点击仅提示（面板既有 `hasWritePermission`）；
   - 配置 `requireWritePermission=true` 后：Viewer 点击 → toast「当前角色无弹窗操作权限」不开窗；
   - Operator/Admin：面板写操作正常下发（SignalR 写链路）；
4. **操作日志**：开关弹窗日志出现在系统日志（源「组态运行」）。

**【验证】** 上述 4 组场景逐条通过（截图或日志留证）。

### 步骤 2.6 阶段验证：播放器全链路回归

**【任务】** 阶段收口。

**【具体内容】**
1. `npm run build` 通过；
2. 运行态既有交互回归抽查：写变量按钮（toggle/setBit/点动）、var-display 设值弹窗、页面跳转、run-script 按钮——行为与改动前一致（CanvasPanel/handleTriggerToggleValue 链路零改动，防御性抽查）；
3. 移动端（浏览器窄屏或 DevTools 设备模拟）：弹窗 92vw 布局正常、文字无缩放失真。

---

## 阶段 3：预览宿主与既有缺口收口（3 步）

### 步骤 3.1 预览端前置检查

**【任务】** 确认 `ScadaTopologyView` 预览态的设备订阅与组件解析机制，决定弹窗数据策略。

**【具体内容】**
1. 检索 `ScadaTopologyView.vue` 中 `subscribeDeviceTelemetry` / `boundDeviceIds` 同类逻辑：
   - **若有**等价订阅集合 → 步骤 3.2 照搬阶段 2 的差集订阅模式；
   - **若无**（预览态依赖编辑器全局已加载的 devices store） → 预览弹窗数据可能 stale，属可接受（预览用途；文档注明），订阅差集可省略；
2. 确认预览态 `componentValues` 与 `currentPage` 的 computed 结构（供 openPopup 查找组件复用）；
3. 输出检查结论（写进执行记录，供 3.2 按结论实施）。

**【验证】** 结论明确（有/无订阅机制二选一），3.2 无歧义。

### 步骤 3.2 `ScadaTopologyView.vue`：预览态弹窗实现

**【任务】** 编辑器预览（isActiveMode）获得与运行态一致的弹窗体验（P2/E6）。

**【改动文件】** `Client/src/components/ScadaTopologyView.vue`

**【具体内容】**（按 3.1 结论裁剪）：
1. import `PopupHost`（及按需 `collectComponentDeviceRefs`）；
2. 复制阶段 2 的弹窗状态区（`activePopupId` / `activePopupComponent` / `openPopup` / `closePopup`），
   组件查找用本视图的 `currentPage`；`canControlWrite` 已存在（预览事件上下文在用）；
3. `previewEventCtx`（约 L854）追加 `openPopup` 成员（函数引用同阶段 2 模式）；
4. 模板挂载 `PopupHost`：预览画布容器外同级（与阶段 2 挂载原则一致）；
5. 切页关闭：预览态切页入口若与播放器不同（编辑器页面切换），在对应 watch/处理器中补 `closePopup()`；
6. 若 3.1 结论为"有订阅机制" → 补差集订阅 watch；否则跳过并在弹窗打开时 `addLog('组态拓扑', '预览弹窗数据可能非实时', 'normal')` 一次性提示。

**【验证】** 编辑器进入预览（isActiveMode）→ 点击电机 → 弹窗打开/关闭/权限表现与播放器一致；退出预览后弹窗不残留（预览切换处理器中关闭）。

**【注意】** 预览宿主实现为**推荐项**：若时间紧可第一版仅依赖 D2 判空降级（toast「当前宿主不支持弹窗动作」），不阻塞主线——但推荐做全，编辑器内所见即所得是本方案体验卖点。

### 步骤 3.3 `boundDeviceIds` 既有缺口收口

**【任务】** 修复评审 P1 发现的既有缺陷：vfd-motor-panel 变量级设备引用不入订阅。

**【改动文件】** `Client/src/components/ScadaPlayerView.vue`（`boundDeviceIds` computed，约 L92）

**【具体内容】** 在 forEach 内追加通用收集（保留既有四类分支不动，避免行为回退）：

```ts
  (currentPage.value?.components ?? []).forEach((c: any) => {
    // …既有 bindDeviceId / opDeviceId / dashboardItems / trendSeries 分支保持不变…

    // P1 收口：通用设备引用收集（覆盖 vfd-motor-panel 等 props.xxxDeviceId 变量级绑定，
    // 未来组件新增 DeviceId 后缀字段自动生效）
    collectComponentDeviceRefs(c).forEach((id) => ids.add(id));
  });
```

> 说明：`collectComponentDeviceRefs` 的 `bindDeviceId` 部分与既有首分支重复 add（Set 幂等，无副作用）；
> `opDeviceId` 与 rounded-btn 分支重复同理。保留既有分支是为了让 trend-chart/dashboard 的**回退逻辑**
> （无绑定序列订阅首台设备）不受影响。

**【验证】**
1. 编辑器：画布放一块 `vfd-motor-panel`，`spFreqDeviceId` 指向设备 B（不在页面其它组件绑定中）；
2. 播放器（不点弹窗，直接看画布面板）：修改设备 B 的 `sp_freq` → 画布面板 SP 值实时变化（收口前 stale）；
3. 弹窗场景复验（步骤 2.4 验证 2）——此时设备 B 已进主订阅集合，弹窗差集 watch 走"主集合已覆盖"分支，双保险。

**【注意】** 此步会**扩大订阅范围**（画布面板跨设备绑定也从 stale 变实时），属缺陷修复非行为变更；回归确认订阅数量无异常膨胀（网络面板 SignalR 分组数）。

---

## 阶段 4：回归验收与收尾（2 步）

### 步骤 4.1 构建与全量回归验收

**【任务】** 对照 01-方案设计 §11 验收清单全量勾验。

**【具体内容】**
1. 构建：`npm run build`（前端）；防御性 `dotnet build`（后端零改动确认）；
2. 按 01-方案设计 §11「功能 / 数据 / 权限 / 边界 / 回归」五组 19 条逐条勾验（每条留证：截图/日志/操作记录）；
3. 回归重点（与既有硬约束相关）：
   - 事件面板四类既有动作配置编辑正常；
   - 运行态写变量 / 设值弹窗 / 脚本按钮 / 页面跳转不回归；
   - 编辑器 Ctrl+C/X/V/Z 快捷键仅设计模式可用（本功能未触碰键盘链路，防御性确认）；
   - 页面保存/重载后 events（含 openPopup）配置无损。

### 步骤 4.2 文档与提交收尾

**【任务】** 文档状态更新与分阶段提交。

**【具体内容】**
1. 更新 `docs/hmi-runtime-popup/README.md` 状态勾选（待评审确认 → 已实施）；
2. 新增执行记录要点（实施中的偏差、3.1 检查结论、验收结果）追加至本计划或独立 `04-执行记录.md`；
3. 按阶段分批提交（每阶段一 commit，便于回滚定位）：
   - `feat(hmi): openPopup 事件动作类型与事件面板配置（阶段1）`
   - `feat(hmi): 运行态弹窗 PopupHost 与播放器集成（阶段2）`
   - `feat(hmi): 预览宿主弹窗与 boundDeviceIds 设备引用收口（阶段3）`
   - `docs(hmi): 运行态弹窗方案文档与执行记录（阶段4）`

**【验证】** git log 分阶段清晰；工作区无未跟踪残留（样例工程改动属组态数据，不入库）。

---

## 附 A：风险与回滚

| 风险 | 概率 | 影响 | 缓解 |
| --- | --- | --- | --- |
| 弹窗设备订阅与主订阅对账冲突（重复订阅/漏退订） | 中 | 弹窗数据 stale 或订阅泄漏 | 差集式只做增量；验收清单含退订检查；阶段独立提交可回滚 |
| `collectComponentDeviceRefs` 误收（props 恰有非设备含义的 `xxxDeviceId` 字段） | 低 | 多余订阅（无副作用，仅多一路推送） | 字段命名约定审查；Set 幂等；监控订阅数 |
| 预览宿主结构差异导致挂载位置不当 | 中 | 预览弹窗样式异常 | 步骤 3.1 前置检查；预览实现为可选项，可降级 toast 不阻塞 |
| 浅拷贝 props 导致面板内部状态互相污染 | 低 | 画布源面板状态异常 | vfd-motor-panel 本地状态存自身 ref 不落 props；验收含画布面板回归 |
| 既有事件动作行为回退 | 低 | 存量组态交互失效 | CanvasPanel/handleTriggerToggleValue 零改动；阶段 2/4 双回归 |

回滚策略：每阶段独立 commit；任一阶段异常可单独 revert（阶段 1 类型扩展向后兼容，revert 阶段 2/3 后配置数据仍在 PropsJson 中 dormant，无害）。

## 附 B：工作量估算

| 阶段 | 新增/修改代码量 | 预估工时 |
| --- | --- | --- |
| 阶段 0 | 0（记录） | 0.5h |
| 阶段 1 | ~60 行 | 1h |
| 阶段 2 | ~285 行（含两个新文件） | 3h |
| 阶段 3 | ~45 行 | 1.5h |
| 阶段 4 | 0（验证+文档） | 1.5h |
| **合计** | **~390 行** | **约 7.5h** |