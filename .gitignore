# ── OS files ──
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini
.workbuddy/

# ── IDE / Editor ──
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
*.swp
*.swo
*~

# ── Logs ──
*.log

# ── Environment ──
.env*
!.env.example

# ── Runtime-generated config ──
# 数据库管理页运行时生成的主库覆盖文件（含数据库密码），不入库
appsettings.dboverride.json

# ── Runtime uploads ──
# 组态图片图库：运行时上传的用户图片（GUID_原名），不入库
Server/ScadaServer.WebApi/uploads/

# ── db-refactor 键值快照基线 ──
# 阶段0/6.1 约定：Devices/ModelVariables/DeviceVariables 键值快照存本地不入库
docs/db-refactor/_baseline/
